# Vento - Estructura Base del Proyecto

¡Excelente arquitectura! Vamos a crear una base sólida para Vento.


---

## 🔧 Script de Bash - Crear Estructura

```bash
#!/bin/bash
#==============================================================================
# create_shopvibe.sh
# Script para crear la estructura completa del proyecto ShopVibe
#==============================================================================

set -e  # Salir si hay errores

PROJECT_NAME="ShopVibe"
PROJECT_DIR="./${PROJECT_NAME}"

echo "╔══════════════════════════════════════════════════════════════╗"
echo "║           🛒 Creando proyecto ShopVibe                       ║"
echo "║           Arquitectura Feature-Based con Qt6/C++20           ║"
echo "╚══════════════════════════════════════════════════════════════╝"
echo ""

# Crear directorio principal
if [ -d "$PROJECT_DIR" ]; then
    echo "⚠️  El directorio $PROJECT_NAME ya existe. ¿Sobrescribir? (s/n)"
    read -r response
    if [ "$response" != "s" ]; then
        echo "Cancelado."
        exit 1
    fi
    rm -rf "$PROJECT_DIR"
fi

mkdir -p "$PROJECT_DIR"
cd "$PROJECT_DIR"

echo "📁 Creando estructura de carpetas..."

# ==============================================================================
# Estructura Core
# ==============================================================================
mkdir -p src/core/database
mkdir -p src/core/logger
mkdir -p src/core/utils

# ==============================================================================
# Estructura Features (Feature-Based Architecture)
# ==============================================================================
mkdir -p src/features/currency
mkdir -p src/features/inventory
mkdir -p src/features/sales
mkdir -p src/features/reporting

# ==============================================================================
# Estructura UI
# ==============================================================================
mkdir -p src/ui/qml
mkdir -p src/ui/qml/components
mkdir -p src/ui/qml/views

# ==============================================================================
# Recursos y Tests
# ==============================================================================
mkdir -p resources/icons
mkdir -p resources/fonts
mkdir -p tests

# ==============================================================================
# Build directory (para CMake out-of-source build)
# ==============================================================================
mkdir -p build

echo "✅ Estructura de carpetas creada"
echo ""

# ==============================================================================
# Crear archivos .gitkeep para preservar carpetas vacías
# ==============================================================================
touch resources/icons/.gitkeep
touch resources/fonts/.gitkeep
touch tests/.gitkeep
touch src/features/reporting/.gitkeep

echo "📄 Creando archivos de configuración..."

# ==============================================================================
# .gitignore
# ==============================================================================
cat > .gitignore << 'EOF'
# Build directories
build/
cmake-build-*/
out/

# IDE
.idea/
.vscode/
*.user
*.autosave

# Compiled
*.o
*.obj
*.exe
*.dll
*.so
*.dylib
*.a

# Qt
moc_*.cpp
qrc_*.cpp
ui_*.h
*.qmlc
*.jsc

# SQLite databases (desarrollo)
*.db
*.sqlite

# Logs
*.log

# OS
.DS_Store
Thumbs.db
EOF

# ==============================================================================
# README.md
# ==============================================================================
cat > README.md << 'EOF'
# 🛒 ShopVibe

Sistema de gestión de inventario y ventas con soporte para múltiples monedas.

## 🏗️ Arquitectura

Este proyecto utiliza **Feature-Based Architecture** (Arquitectura por Características),
organizando el código por dominio de negocio en lugar de capas técnicas.

```
src/
├── core/           # Utilidades compartidas (DB, Logger, EventBus)
├── features/       # Dominios de negocio
│   ├── currency/   # Gestión de tipo de cambio
│   ├── inventory/  # Gestión de productos
│   ├── sales/      # Registro de ventas
│   └── reporting/  # Generación de reportes
└── ui/             # Interfaz de usuario (QML)
```

## 🔧 Requisitos

- C++20 compatible compiler (GCC 10+, Clang 10+, MSVC 2019+)
- Qt 6.5+
- CMake 3.21+

## 🚀 Compilación

```bash
mkdir build && cd build
cmake ..
cmake --build .
```

## 📋 Licencia

MIT License
EOF

echo "✅ Archivos de configuración creados"
echo ""
echo "╔══════════════════════════════════════════════════════════════╗"
echo "║           ✅ Proyecto ShopVibe creado exitosamente!          ║"
echo "╚══════════════════════════════════════════════════════════════╝"
echo ""
echo "📍 Ubicación: $(pwd)"
echo ""
echo "Próximos pasos:"
echo "  1. cd $PROJECT_NAME"
echo "  2. Agrega los archivos CMakeLists.txt y código fuente"
echo "  3. mkdir build && cd build"
echo "  4. cmake .."
echo "  5. cmake --build ."
```

---

## 📄 CMakeLists.txt Principal (Raíz)

```cmake
#===============================================================================
# CMakeLists.txt - ShopVibe
# Proyecto principal - Arquitectura Feature-Based
#===============================================================================
cmake_minimum_required(VERSION 3.21)

project(ShopVibe
    VERSION 1.0.0
    DESCRIPTION "Sistema de gestión de inventario y ventas"
    LANGUAGES CXX
)

#===============================================================================
# Configuración de C++20
#===============================================================================
set(CMAKE_CXX_STANDARD 20)
set(CMAKE_CXX_STANDARD_REQUIRED ON)
set(CMAKE_CXX_EXTENSIONS OFF)

# Habilitar exportación de símbolos en Windows
set(CMAKE_WINDOWS_EXPORT_ALL_SYMBOLS ON)

# Compilación posicional independiente (para shared libraries)
set(CMAKE_POSITION_INDEPENDENT_CODE ON)

#===============================================================================
# Configuración de Qt6
#===============================================================================
set(CMAKE_AUTOMOC ON)  # Auto Meta-Object Compiler
set(CMAKE_AUTORCC ON)  # Auto Resource Compiler
set(CMAKE_AUTOUIC ON)  # Auto UI Compiler

# Buscar paquetes Qt6 requeridos
find_package(Qt6 6.5 REQUIRED COMPONENTS
    Core
    Gui
    Qml
    Quick
    QuickControls2
    Sql
    Widgets
)

# Política para versiones de QML
qt_policy(SET QTP0001 NEW)

#===============================================================================
# Opciones de compilación
#===============================================================================
option(SHOPVIBE_BUILD_TESTS "Compilar tests unitarios" OFF)
option(SHOPVIBE_ENABLE_SANITIZERS "Habilitar sanitizers (debug)" OFF)

# Warnings estrictos
if(CMAKE_CXX_COMPILER_ID MATCHES "GNU|Clang")
    add_compile_options(
        -Wall
        -Wextra
        -Wpedantic
        -Werror=return-type
        -Wno-unused-parameter
    )
elseif(MSVC)
    add_compile_options(
        /W4
        /permissive-
        /utf-8
    )
endif()

# Sanitizers (solo en debug)
if(SHOPVIBE_ENABLE_SANITIZERS AND CMAKE_BUILD_TYPE STREQUAL "Debug")
    add_compile_options(-fsanitize=address,undefined)
    add_link_options(-fsanitize=address,undefined)
endif()

#===============================================================================
# Directorios de inclusión globales
#===============================================================================
include_directories(${CMAKE_SOURCE_DIR}/src)

#===============================================================================
# Variables para organización de archivos
#===============================================================================
set(SHOPVIBE_SRC_DIR ${CMAKE_SOURCE_DIR}/src)
set(SHOPVIBE_CORE_DIR ${SHOPVIBE_SRC_DIR}/core)
set(SHOPVIBE_FEATURES_DIR ${SHOPVIBE_SRC_DIR}/features)
set(SHOPVIBE_UI_DIR ${SHOPVIBE_SRC_DIR}/ui)

#===============================================================================
# Subdirectorios del proyecto
#===============================================================================
add_subdirectory(src)

#===============================================================================
# Tests (opcional)
#===============================================================================
if(SHOPVIBE_BUILD_TESTS)
    enable_testing()
    add_subdirectory(tests)
endif()

#===============================================================================
# Información de compilación
#===============================================================================
message(STATUS "")
message(STATUS "╔═══════════════════════════════════════════════════════════╗")
message(STATUS "║              ShopVibe - Configuración                     ║")
message(STATUS "╠═══════════════════════════════════════════════════════════╣")
message(STATUS "║  Versión:        ${PROJECT_VERSION}")
message(STATUS "║  Compilador:     ${CMAKE_CXX_COMPILER_ID} ${CMAKE_CXX_COMPILER_VERSION}")
message(STATUS "║  Estándar C++:   ${CMAKE_CXX_STANDARD}")
message(STATUS "║  Qt Version:     ${Qt6_VERSION}")
message(STATUS "║  Build Type:     ${CMAKE_BUILD_TYPE}")
message(STATUS "║  Tests:          ${SHOPVIBE_BUILD_TESTS}")
message(STATUS "╚═══════════════════════════════════════════════════════════╝")
message(STATUS "")
```

---

## 📄 src/CMakeLists.txt

```cmake
#===============================================================================
# src/CMakeLists.txt
# Configuración de subdirectorios y ejecutable principal
#===============================================================================

#===============================================================================
# Agregar bibliotecas de cada módulo
#===============================================================================

# Core - Utilidades compartidas
add_subdirectory(core)

# Features - Módulos de negocio
add_subdirectory(features)

# UI - Interfaz de usuario
add_subdirectory(ui)

#===============================================================================
# Ejecutable principal
#===============================================================================
qt_add_executable(${PROJECT_NAME}
    main.cpp
)

#===============================================================================
# Linkear bibliotecas
#===============================================================================
target_link_libraries(${PROJECT_NAME} PRIVATE
    # Qt6
    Qt6::Core
    Qt6::Gui
    Qt6::Qml
    Qt6::Quick
    Qt6::QuickControls2
    Qt6::Sql
    Qt6::Widgets
    
    # Módulos internos
    ShopVibe::Core
    ShopVibe::Features
    ShopVibe::UI
)

#===============================================================================
# Propiedades del ejecutable
#===============================================================================
set_target_properties(${PROJECT_NAME} PROPERTIES
    MACOSX_BUNDLE TRUE
    WIN32_EXECUTABLE TRUE
    OUTPUT_NAME "ShopVibe"
)

# Copiar QML a directorio de build (desarrollo)
if(CMAKE_BUILD_TYPE STREQUAL "Debug")
    add_custom_command(TARGET ${PROJECT_NAME} POST_BUILD
        COMMAND ${CMAKE_COMMAND} -E copy_directory
        ${CMAKE_SOURCE_DIR}/src/ui/qml
        $<TARGET_FILE_DIR:${PROJECT_NAME}>/qml
    )
endif()
```

---

## 📄 src/core/CMakeLists.txt

```cmake
#===============================================================================
# src/core/CMakeLists.txt
# Biblioteca Core - Utilidades compartidas
#===============================================================================

set(CORE_SOURCES
    database/DatabaseManager.cpp
    logger/Logger.cpp
)

set(CORE_HEADERS
    database/DatabaseManager.hpp
    logger/Logger.hpp
    utils/EventBus.hpp
    utils/Types.hpp
)

#===============================================================================
# Biblioteca estática Core
#===============================================================================
add_library(ShopVibeCore STATIC
    ${CORE_SOURCES}
    ${CORE_HEADERS}
)

# Alias para uso limpio
add_library(ShopVibe::Core ALIAS ShopVibeCore)

#===============================================================================
# Configuración de la biblioteca
#===============================================================================
target_include_directories(ShopVibeCore
    PUBLIC
        ${CMAKE_CURRENT_SOURCE_DIR}
        ${CMAKE_CURRENT_SOURCE_DIR}/..
)

target_link_libraries(ShopVibeCore
    PUBLIC
        Qt6::Core
        Qt6::Sql
)

# Features de C++20
target_compile_features(ShopVibeCore PUBLIC cxx_std_20)
```

---

## 📄 src/features/CMakeLists.txt

```cmake
#===============================================================================
# src/features/CMakeLists.txt
# Módulos de características de negocio
#===============================================================================

# Agregar cada feature como submódulo
add_subdirectory(currency)
add_subdirectory(inventory)
add_subdirectory(sales)

#===============================================================================
# Biblioteca agregadora de Features
#===============================================================================
add_library(ShopVibeFeatures INTERFACE)
add_library(ShopVibe::Features ALIAS ShopVibeFeatures)

target_link_libraries(ShopVibeFeatures
    INTERFACE
        ShopVibe::Currency
        ShopVibe::Inventory
        ShopVibe::Sales
)
```

---

## 📄 src/features/currency/CMakeLists.txt

```cmake
#===============================================================================
# src/features/currency/CMakeLists.txt
# Feature: Gestión de tipo de cambio
#===============================================================================

set(CURRENCY_SOURCES
    CurrencyService.cpp
)

set(CURRENCY_HEADERS
    CurrencyService.hpp
    CurrencyModel.hpp
)

add_library(ShopVibeCurrency STATIC
    ${CURRENCY_SOURCES}
    ${CURRENCY_HEADERS}
)

add_library(ShopVibe::Currency ALIAS ShopVibeCurrency)

target_include_directories(ShopVibeCurrency
    PUBLIC
        ${CMAKE_CURRENT_SOURCE_DIR}
)

target_link_libraries(ShopVibeCurrency
    PUBLIC
        Qt6::Core
        Qt6::Qml
        ShopVibe::Core
)
```

---

## 📄 src/features/inventory/CMakeLists.txt

```cmake
#===============================================================================
# src/features/inventory/CMakeLists.txt
# Feature: Gestión de inventario
#===============================================================================

set(INVENTORY_SOURCES
    InventoryService.cpp
)

set(INVENTORY_HEADERS
    InventoryService.hpp
    ProductModel.hpp
)

add_library(ShopVibeInventory STATIC
    ${INVENTORY_SOURCES}
    ${INVENTORY_HEADERS}
)

add_library(ShopVibe::Inventory ALIAS ShopVibeInventory)

target_include_directories(ShopVibeInventory
    PUBLIC
        ${CMAKE_CURRENT_SOURCE_DIR}
)

target_link_libraries(ShopVibeInventory
    PUBLIC
        Qt6::Core
        Qt6::Qml
        ShopVibe::Core
        ShopVibe::Currency
)
```

---

## 📄 src/features/sales/CMakeLists.txt

```cmake
#===============================================================================
# src/features/sales/CMakeLists.txt
# Feature: Registro de ventas
#===============================================================================

set(SALES_SOURCES
    SalesService.cpp
)

set(SALES_HEADERS
    SalesService.hpp
    SaleModel.hpp
)

add_library(ShopVibeSales STATIC
    ${SALES_SOURCES}
    ${SALES_HEADERS}
)

add_library(ShopVibe::Sales ALIAS ShopVibeSales)

target_include_directories(ShopVibeSales
    PUBLIC
        ${CMAKE_CURRENT_SOURCE_DIR}
)

target_link_libraries(ShopVibeSales
    PUBLIC
        Qt6::Core
        Qt6::Qml
        ShopVibe::Core
        ShopVibe::Inventory
)
```

---

## 📄 src/ui/CMakeLists.txt

```cmake
#===============================================================================
# src/ui/CMakeLists.txt
# Módulo de interfaz de usuario QML
#===============================================================================

#===============================================================================
# Módulo QML usando qt_add_qml_module
#===============================================================================
qt_add_library(ShopVibeUI STATIC)
add_library(ShopVibe::UI ALIAS ShopVibeUI)

qt_add_qml_module(ShopVibeUI
    URI ShopVibe.UI
    VERSION 1.0
    QML_FILES
        qml/Main.qml
        qml/components/AppHeader.qml
        qml/components/SideNavigation.qml
        qml/views/DashboardView.qml
    RESOURCES
        qml/qml.qrc
    OUTPUT_DIRECTORY
        ${CMAKE_BINARY_DIR}/ShopVibe/UI
)

target_include_directories(ShopVibeUI
    PUBLIC
        ${CMAKE_CURRENT_SOURCE_DIR}
)

target_link_libraries(ShopVibeUI
    PUBLIC
        Qt6::Core
        Qt6::Gui
        Qt6::Qml
        Qt6::Quick
        Qt6::QuickControls2
)
```

---

## 📄 src/main.cpp

```cpp
//==============================================================================
// main.cpp
// ShopVibe - Entry Point
// Arquitectura Feature-Based con Qt6/C++20
//==============================================================================

#include <QGuiApplication>
#include <QQmlApplicationEngine>
#include <QQuickStyle>
#include <QIcon>
#include <QFont>
#include <QDir>
#include <QDebug>

// Core
#include "core/database/DatabaseManager.hpp"
#include "core/logger/Logger.hpp"

// Features (para registro en QML)
#include "features/currency/CurrencyService.hpp"
#include "features/inventory/InventoryService.hpp"
#include "features/sales/SalesService.hpp"

//==============================================================================
// Configuración de la aplicación
//==============================================================================
namespace Config {
    constexpr auto APP_NAME = "ShopVibe";
    constexpr auto ORG_NAME = "ShopVibeTeam";
    constexpr auto ORG_DOMAIN = "shopvibe.app";
    constexpr auto APP_VERSION = "1.0.0";
}

//==============================================================================
// Registro de tipos QML
//==============================================================================
void registerQmlTypes() {
    // Currency Feature
    qmlRegisterSingletonType<ShopVibe::CurrencyService>(
        "ShopVibe.Currency", 1, 0, "CurrencyService",
        [](QQmlEngine* engine, QJSEngine* scriptEngine) -> QObject* {
            Q_UNUSED(engine)
            Q_UNUSED(scriptEngine)
            return &ShopVibe::CurrencyService::instance();
        }
    );
    
    // Inventory Feature
    qmlRegisterSingletonType<ShopVibe::InventoryService>(
        "ShopVibe.Inventory", 1, 0, "InventoryService",
        [](QQmlEngine* engine, QJSEngine* scriptEngine) -> QObject* {
            Q_UNUSED(engine)
            Q_UNUSED(scriptEngine)
            return &ShopVibe::InventoryService::instance();
        }
    );
    
    // Sales Feature
    qmlRegisterSingletonType<ShopVibe::SalesService>(
        "ShopVibe.Sales", 1, 0, "SalesService",
        [](QQmlEngine* engine, QJSEngine* scriptEngine) -> QObject* {
            Q_UNUSED(engine)
            Q_UNUSED(scriptEngine)
            return &ShopVibe::SalesService::instance();
        }
    );
    
    qInfo() << "✓ Tipos QML registrados correctamente";
}

//==============================================================================
// Configuración del estilo visual
//==============================================================================
void setupApplicationStyle() {
    // Usar estilo Material para look moderno tipo Windows 11
    // Opciones: "Material", "Fusion", "Universal", "Windows"
    QQuickStyle::setStyle("Material");
    
    // Variante del tema Material
    // Opciones: "Normal", "Dense"
    qputenv("QT_QUICK_CONTROLS_MATERIAL_VARIANT", "Dense");
    
    // Tema oscuro/claro
    // Opciones: "Light", "Dark", "System"
    qputenv("QT_QUICK_CONTROLS_MATERIAL_THEME", "Light");
    
    // Color de acento (azul Windows 11)
    qputenv("QT_QUICK_CONTROLS_MATERIAL_ACCENT", "#0078D4");
    
    // Color primario
    qputenv("QT_QUICK_CONTROLS_MATERIAL_PRIMARY", "#005A9E");
    
    qInfo() << "✓ Estilo Material configurado";
}

//==============================================================================
// Inicialización de servicios
//==============================================================================
bool initializeServices() {
    // Inicializar Logger
    ShopVibe::Logger::instance().setLogLevel(ShopVibe::LogLevel::Debug);
    ShopVibe::Logger::instance().info("ShopVibe iniciando...");
    
    // Inicializar base de datos
    const QString dbPath = QDir::currentPath() + "/shopvibe.db";
    if (!ShopVibe::DatabaseManager::instance().initialize(dbPath)) {
        ShopVibe::Logger::instance().error("Error al inicializar base de datos");
        return false;
    }
    
    // Inicializar servicios de features
    ShopVibe::CurrencyService::instance().initialize();
    ShopVibe::InventoryService::instance().initialize();
    ShopVibe::SalesService::instance().initialize();
    
    ShopVibe::Logger::instance().info("✓ Todos los servicios inicializados");
    return true;
}

//==============================================================================
// Main Entry Point
//==============================================================================
int main(int argc, char *argv[]) {
    //--------------------------------------------------------------------------
    // Configuración previa a QGuiApplication
    //--------------------------------------------------------------------------
    
    // Habilitar High DPI scaling
    QGuiApplication::setHighDpiScaleFactorRoundingPolicy(
        Qt::HighDpiScaleFactorRoundingPolicy::PassThrough
    );
    
    //--------------------------------------------------------------------------
    // Crear aplicación
    //--------------------------------------------------------------------------
    QGuiApplication app(argc, argv);
    
    // Metadatos de la aplicación
    app.setApplicationName(Config::APP_NAME);
    app.setOrganizationName(Config::ORG_NAME);
    app.setOrganizationDomain(Config::ORG_DOMAIN);
    app.setApplicationVersion(Config::APP_VERSION);
    
    // Icono de la aplicación
    app.setWindowIcon(QIcon(":/icons/app_icon.png"));
    
    //--------------------------------------------------------------------------
    // Configurar estilo visual
    //--------------------------------------------------------------------------
    setupApplicationStyle();
    
    //--------------------------------------------------------------------------
    // Registrar tipos QML
    //--------------------------------------------------------------------------
    registerQmlTypes();
    
    //--------------------------------------------------------------------------
    // Inicializar servicios
    //--------------------------------------------------------------------------
    if (!initializeServices()) {
        qCritical() << "Error crítico: No se pudieron inicializar los servicios";
        return -1;
    }
    
    //--------------------------------------------------------------------------
    // Crear motor QML
    //--------------------------------------------------------------------------
    QQmlApplicationEngine engine;
    
    // Agregar rutas de importación QML
    engine.addImportPath(":/");
    engine.addImportPath(QDir::currentPath() + "/qml");
    
    // Exponer propiedades globales al contexto QML
    engine.rootContext()->setContextProperty("appVersion", Config::APP_VERSION);
    engine.rootContext()->setContextProperty("appName", Config::APP_NAME);
    
    //--------------------------------------------------------------------------
    // Cargar QML principal
    //--------------------------------------------------------------------------
    const QUrl mainQml(QStringLiteral("qrc:/qml/Main.qml"));
    
    // Conexión para manejar errores de carga
    QObject::connect(
        &engine,
        &QQmlApplicationEngine::objectCreationFailed,
        &app,
        []() { 
            qCritical() << "Error: No se pudo crear el objeto QML";
            QCoreApplication::exit(-1); 
        },
        Qt::QueuedConnection
    );
    
    engine.load(mainQml);
    
    // Verificar que se cargó correctamente
    if (engine.rootObjects().isEmpty()) {
        qCritical() << "Error: No se cargaron objetos QML";
        return -1;
    }
    
    qInfo() << "╔═══════════════════════════════════════════════════════════╗";
    qInfo() << "║               🛒 ShopVibe v" << Config::APP_VERSION << " iniciado";
    qInfo() << "╚═══════════════════════════════════════════════════════════╝";
    
    //--------------------------------------------------------------------------
    // Ejecutar loop principal
    //--------------------------------------------------------------------------
    return app.exec();
}
```

---

## 📄 src/ui/qml/Main.qml

```qml
//==============================================================================
// Main.qml
// ShopVibe - Ventana principal de la aplicación
//==============================================================================

import QtQuick
import QtQuick.Window
import QtQuick.Controls
import QtQuick.Controls.Material
import QtQuick.Layouts

ApplicationWindow {
    id: mainWindow
    
    //--------------------------------------------------------------------------
    // Propiedades de la ventana
    //--------------------------------------------------------------------------
    width: 1280
    height: 720
    minimumWidth: 800
    minimumHeight: 600
    visible: true
    title: qsTr("ShopVibe - Sistema de Gestión")
    
    //--------------------------------------------------------------------------
    // Tema Material (Windows 11 Style)
    //--------------------------------------------------------------------------
    Material.theme: Material.Light
    Material.accent: "#0078D4"      // Azul Windows 11
    Material.primary: "#005A9E"
    Material.foreground: "#1A1A1A"
    Material.background: "#F3F3F3"
    
    //--------------------------------------------------------------------------
    // Propiedades personalizadas
    //--------------------------------------------------------------------------
    property string currentView: "dashboard"
    property bool sidebarCollapsed: false
    
    //--------------------------------------------------------------------------
    // Paleta de colores de la aplicación
    //--------------------------------------------------------------------------
    readonly property var appColors: ({
        primary: "#0078D4",
        primaryDark: "#005A9E",
        secondary: "#6B6B6B",
        success: "#107C10",
        warning: "#FF8C00",
        danger: "#D13438",
        surface: "#FFFFFF",
        background: "#F3F3F3",
        sidebar: "#2D2D2D",
        text: "#1A1A1A",
        textSecondary: "#6B6B6B"
    })
    
    //--------------------------------------------------------------------------
    // Fondo de la aplicación
    //--------------------------------------------------------------------------
    background: Rectangle {
        color: appColors.background
    }
    
    //--------------------------------------------------------------------------
    // Header de la aplicación
    //--------------------------------------------------------------------------
    header: ToolBar {
        id: appHeader
        height: 48
        
        Material.elevation: 2
        Material.background: appColors.surface
        
        RowLayout {
            anchors.fill: parent
            anchors.leftMargin: 16
            anchors.rightMargin: 16
            spacing: 16
            
            // Botón de menú hamburguesa
            ToolButton {
                icon.source: "qrc:/icons/menu.svg"
                icon.width: 20
                icon.height: 20
                onClicked: sidebarCollapsed = !sidebarCollapsed
                
                ToolTip.visible: hovered
                ToolTip.text: sidebarCollapsed ? "Expandir menú" : "Colapsar menú"
            }
            
            // Logo y título
            Row {
                spacing: 8
                
                Label {
                    text: "🛒"
                    font.pixelSize: 20
                    anchors.verticalCenter: parent.verticalCenter
                }
                
                Label {
                    text: "ShopVibe"
                    font.pixelSize: 18
                    font.weight: Font.DemiBold
                    color: appColors.primary
                    anchors.verticalCenter: parent.verticalCenter
                }
            }
            
            // Espaciador
            Item { Layout.fillWidth: true }
            
            // Barra de búsqueda
            TextField {
                id: searchField
                Layout.preferredWidth: 300
                placeholderText: "🔍 Buscar productos, ventas..."
                
                background: Rectangle {
                    radius: 8
                    color: appColors.background
                    border.color: searchField.activeFocus ? 
                                  appColors.primary : "transparent"
                    border.width: 2
                }
            }
            
            // Espaciador
            Item { Layout.fillWidth: true }
            
            // Indicador de tasa de cambio
            Row {
                spacing: 8
                
                Label {
                    text: "💵 USD:"
                    color: appColors.textSecondary
                    anchors.verticalCenter: parent.verticalCenter
                }
                
                Label {
                    id: exchangeRateLabel
                    text: "Bs. 36.50"  // Se conectará al servicio
                    font.weight: Font.Bold
                    color: appColors.success
                    anchors.verticalCenter: parent.verticalCenter
                }
            }
            
            // Botones de acción
            ToolButton {
                icon.source: "qrc:/icons/notifications.svg"
                icon.width: 20
                icon.height: 20
                
                ToolTip.visible: hovered
                ToolTip.text: "Notificaciones"
            }
            
            ToolButton {
                icon.source: "qrc:/icons/settings.svg"
                icon.width: 20
                icon.height: 20
                
                ToolTip.visible: hovered
                ToolTip.text: "Configuración"
            }
        }
    }
    
    //--------------------------------------------------------------------------
    // Contenido principal con sidebar
    //--------------------------------------------------------------------------
    RowLayout {
        anchors.fill: parent
        spacing: 0
        
        //----------------------------------------------------------------------
        // Sidebar de navegación
        //----------------------------------------------------------------------
        Rectangle {
            id: sidebar
            Layout.preferredWidth: sidebarCollapsed ? 60 : 240
            Layout.fillHeight: true
            color: appColors.sidebar
            
            Behavior on Layout.preferredWidth {
                NumberAnimation { duration: 200; easing.type: Easing.OutCubic }
            }
            
            ColumnLayout {
                anchors.fill: parent
                anchors.topMargin: 16
                spacing: 4
                
                // Items de navegación
                Repeater {
                    model: [
                        { icon: "📊", text: "Dashboard", view: "dashboard" },
                        { icon: "💱", text: "Tipo de Cambio", view: "currency" },
                        { icon: "📦", text: "Inventario", view: "inventory" },
                        { icon: "🛒", text: "Ventas", view: "sales" },
                        { icon: "📈", text: "Reportes", view: "reports" }
                    ]
                    
                    delegate: ItemDelegate {
                        Layout.fillWidth: true
                        Layout.preferredHeight: 48
                        
                        highlighted: currentView === modelData.view
                        
                        contentItem: Row {
                            spacing: sidebarCollapsed ? 0 : 12
                            anchors.centerIn: parent
                            
                            Label {
                                text: modelData.icon
                                font.pixelSize: 18
                                anchors.verticalCenter: parent.verticalCenter
                            }
                            
                            Label {
                                text: modelData.text
                                color: "white"
                                font.pixelSize: 14
                                visible: !sidebarCollapsed
                                opacity: sidebarCollapsed ? 0 : 1
                                anchors.verticalCenter: parent.verticalCenter
                                
                                Behavior on opacity {
                                    NumberAnimation { duration: 150 }
                                }
                            }
                        }
                        
                        background: Rectangle {
                            color: {
                                if (currentView === modelData.view)
                                    return appColors.primary
                                if (parent.hovered)
                                    return Qt.rgba(1, 1, 1, 0.1)
                                return "transparent"
                            }
                            radius: 8
                            anchors.margins: 8
                        }
                        
                        onClicked: currentView = modelData.view
                        
                        ToolTip.visible: sidebarCollapsed && hovered
                        ToolTip.text: modelData.text
                    }
                }
                
                // Espaciador
                Item { Layout.fillHeight: true }
                
                // Versión de la app
                Label {
                    text: sidebarCollapsed ? "v1" : ("v" + appVersion)
                    color: Qt.rgba(1, 1, 1, 0.5)
                    font.pixelSize: 12
                    Layout.alignment: Qt.AlignHCenter
                    Layout.bottomMargin: 16
                }
            }
        }
        
        //----------------------------------------------------------------------
        // Área de contenido principal
        //----------------------------------------------------------------------
        Rectangle {
            Layout.fillWidth: true
            Layout.fillHeight: true
            color: appColors.background
            
            StackLayout {
                anchors.fill: parent
                anchors.margins: 24
                currentIndex: {
                    switch(currentView) {
                        case "dashboard": return 0
                        case "currency": return 1
                        case "inventory": return 2
                        case "sales": return 3
                        case "reports": return 4
                        default: return 0
                    }
                }
                
                // Dashboard View
                Pane {
                    Material.elevation: 2
                    Material.background: appColors.surface
                    
                    ColumnLayout {
                        anchors.centerIn: parent
                        spacing: 16
                        
                        Label {
                            text: "📊 Dashboard"
                            font.pixelSize: 32
                            font.weight: Font.Bold
                            color: appColors.primary
                            Layout.alignment: Qt.AlignHCenter
                        }
                        
                        Label {
                            text: "Bienvenido a ShopVibe"
                            font.pixelSize: 18
                            color: appColors.textSecondary
                            Layout.alignment: Qt.AlignHCenter
                        }
                        
                        Label {
                            text: "El sistema se ha inicializado correctamente ✓"
                            color: appColors.success
                            Layout.alignment: Qt.AlignHCenter
                        }
                    }
                }
                
                // Currency View (placeholder)
                Pane {
                    Material.elevation: 2
                    Label { 
                        anchors.centerIn: parent
                        text: "💱 Tipo de Cambio - En desarrollo"
                        font.pixelSize: 24
                    }
                }
                
                // Inventory View (placeholder)
                Pane {
                    Material.elevation: 2
                    Label { 
                        anchors.centerIn: parent
                        text: "📦 Inventario - En desarrollo"
                        font.pixelSize: 24
                    }
                }
                
                // Sales View (placeholder)
                Pane {
                    Material.elevation: 2
                    Label { 
                        anchors.centerIn: parent
                        text: "🛒 Ventas - En desarrollo"
                        font.pixelSize: 24
                    }
                }
                
                // Reports View (placeholder)
                Pane {
                    Material.elevation: 2
                    Label { 
                        anchors.centerIn: parent
                        text: "📈 Reportes - En desarrollo"
                        font.pixelSize: 24
                    }
                }
            }
        }
    }
    
    //--------------------------------------------------------------------------
    // Barra de estado (footer)
    //--------------------------------------------------------------------------
    footer: ToolBar {
        height: 28
        Material.background: appColors.surface
        
        RowLayout {
            anchors.fill: parent
            anchors.leftMargin: 16
            anchors.rightMargin: 16
            
            Label {
                text: "✓ Conectado a base de datos"
                font.pixelSize: 12
                color: appColors.success
            }
            
            Item { Layout.fillWidth: true }
            
            Label {
                text: Qt.formatDateTime(new Date(), "dd/MM/yyyy hh:mm")
                font.pixelSize: 12
                color: appColors.textSecondary
            }
        }
    }
}
```

---

## 📄 src/ui/qml/qml.qrc

```xml
<!DOCTYPE RCC>
<RCC version="1.0">
    <qresource prefix="/">
        <file>qml/Main.qml</file>
        <file>qml/components/AppHeader.qml</file>
        <file>qml/components/SideNavigation.qml</file>
        <file>qml/views/DashboardView.qml</file>
    </qresource>
    <qresource prefix="/icons">
        <!-- Agregar iconos SVG aquí -->
    </qresource>
</RCC>
```

---

## 📄 Archivos Placeholder para Core y Features

### src/core/database/DatabaseManager.hpp

```cpp
//==============================================================================
// DatabaseManager.hpp
// Gestor de conexión SQLite
//==============================================================================
#pragma once

#include <QObject>
#include <QSqlDatabase>
#include <QString>
#include <memory>

namespace ShopVibe {

class DatabaseManager : public QObject {
    Q_OBJECT
    
public:
    static DatabaseManager& instance();
    
    bool initialize(const QString& dbPath);
    bool isConnected() const;
    QSqlDatabase& database();
    
    // Prevent copying
    DatabaseManager(const DatabaseManager&) = delete;
    DatabaseManager& operator=(const DatabaseManager&) = delete;
    
private:
    DatabaseManager() = default;
    ~DatabaseManager() = default;
    
    QSqlDatabase m_database;
    bool m_connected = false;
};

} // namespace ShopVibe
```

### src/core/database/DatabaseManager.cpp

```cpp
#include "DatabaseManager.hpp"
#include <QSqlError>
#include <QDebug>

namespace ShopVibe {

DatabaseManager& DatabaseManager::instance() {
    static DatabaseManager instance;
    return instance;
}

bool DatabaseManager::initialize(const QString& dbPath) {
    m_database = QSqlDatabase::addDatabase("QSQLITE");
    m_database.setDatabaseName(dbPath);
    
    if (!m_database.open()) {
        qCritical() << "Error DB:" << m_database.lastError().text();
        return false;
    }
    
    m_connected = true;
    qInfo() << "✓ Base de datos conectada:" << dbPath;
    return true;
}

bool DatabaseManager::isConnected() const {
    return m_connected;
}

QSqlDatabase& DatabaseManager::database() {
    return m_database;
}

} // namespace ShopVibe
```

### src/core/logger/Logger.hpp

```cpp
//==============================================================================
// Logger.hpp
// Sistema de logging simple
//==============================================================================
#pragma once

#include <QObject>
#include <QString>

namespace ShopVibe {

enum class LogLevel { Debug, Info, Warning, Error };

class Logger : public QObject {
    Q_OBJECT
    
public:
    static Logger& instance();
    
    void setLogLevel(LogLevel level);
    void debug(const QString& message);
    void info(const QString& message);
    void warning(const QString& message);
    void error(const QString& message);
    
private:
    Logger() = default;
    LogLevel m_level = LogLevel::Info;
};

} // namespace ShopVibe
```

### src/core/logger/Logger.cpp

```cpp
#include "Logger.hpp"
#include <QDebug>
#include <QDateTime>

namespace ShopVibe {

Logger& Logger::instance() {
    static Logger instance;
    return instance;
}

void Logger::setLogLevel(LogLevel level) {
    m_level = level;
}

void Logger::debug(const QString& message) {
    if (m_level <= LogLevel::Debug)
        qDebug() << "[DEBUG]" << message;
}

void Logger::info(const QString& message) {
    if (m_level <= LogLevel::Info)
        qInfo() << "[INFO]" << message;
}

void Logger::warning(const QString& message) {
    if (m_level <= LogLevel::Warning)
        qWarning() << "[WARN]" << message;
}

void Logger::error(const QString& message) {
    qCritical() << "[ERROR]" << message;
}

} // namespace ShopVibe
```

### src/core/utils/EventBus.hpp (Observer Pattern)

```cpp
//==============================================================================
// EventBus.hpp
// Implementación del patrón Observer para comunicación entre features
//==============================================================================
#pragma once

#include <QObject>
#include <QVariant>
#include <functional>
#include <unordered_map>
#include <vector>
#include <string>

namespace ShopVibe {

class EventBus : public QObject {
    Q_OBJECT
    
public:
    using EventHandler = std::function<void(const QVariant&)>;
    using SubscriptionId = size_t;
    
    static EventBus& instance() {
        static EventBus instance;
        return instance;
    }
    
    // Suscribirse a un evento
    SubscriptionId subscribe(const std::string& eventName, EventHandler handler) {
        auto id = m_nextId++;
        m_subscribers[eventName].push_back({id, std::move(handler)});
        return id;
    }
    
    // Publicar un evento
    void publish(const std::string& eventName, const QVariant& data = {}) {
        if (auto it = m_subscribers.find(eventName); it != m_subscribers.end()) {
            for (const auto& [id, handler] : it->second) {
                handler(data);
            }
        }
    }
    
    // Cancelar suscripción
    void unsubscribe(const std::string& eventName, SubscriptionId id) {
        if (auto it = m_subscribers.find(eventName); it != m_subscribers.end()) {
            auto& handlers = it->second;
            handlers.erase(
                std::remove_if(handlers.begin(), handlers.end(),
                    [id](const auto& pair) { return pair.first == id; }),
                handlers.end()
            );
        }
    }
    
signals:
    void currencyRateChanged(double newRate);
    void inventoryUpdated();
    void saleCompleted(int saleId);
    
private:
    EventBus() = default;
    
    std::unordered_map<std::string, 
        std::vector<std::pair<SubscriptionId, EventHandler>>> m_subscribers;
    SubscriptionId m_nextId = 0;
};

// Eventos predefinidos
namespace Events {
    constexpr auto CURRENCY_RATE_CHANGED = "currency.rate.changed";
    constexpr auto INVENTORY_UPDATED = "inventory.updated";
    constexpr auto SALE_COMPLETED = "sale.completed";
}

} // namespace ShopVibe
```

### Placeholders de Features (CurrencyService, InventoryService, SalesService)

```cpp
// src/features/currency/CurrencyService.hpp
#pragma once
#include <QObject>

namespace ShopVibe {
class CurrencyService : public QObject {
    Q_OBJECT
    Q_PROPERTY(double rate READ rate WRITE setRate NOTIFY rateChanged)
    
public:
    static CurrencyService& instance() {
        static CurrencyService inst;
        return inst;
    }
    void initialize() {}
    double rate() const { return m_rate; }
    void setRate(double rate) { m_rate = rate; emit rateChanged(); }
    
signals:
    void rateChanged();
    
private:
    CurrencyService() = default;
    double m_rate = 36.50;
};
}

// src/features/currency/CurrencyService.cpp
#include "CurrencyService.hpp"
```

```cpp
// src/features/inventory/InventoryService.hpp
#pragma once
#include <QObject>

namespace ShopVibe {
class InventoryService : public QObject {
    Q_OBJECT
public:
    static InventoryService& instance() {
        static InventoryService inst;
        return inst;
    }
    void initialize() {}
private:
    InventoryService() = default;
};
}
```

```cpp
// src/features/sales/SalesService.hpp
#pragma once
#include <QObject>

namespace ShopVibe {
class SalesService : public QObject {
    Q_OBJECT
public:
    static SalesService& instance() {
        static SalesService inst;
        return inst;
    }
    void initialize() {}
private:
    SalesService() = default;
};
}
```

---

## 🚀 Comandos para Compilar

```bash
# Crear la estructura
chmod +x create_shopvibe.sh
./create_shopvibe.sh

# Entrar al proyecto
cd ShopVibe

# Compilar
mkdir build && cd build
cmake .. -DCMAKE_BUILD_TYPE=Debug
cmake --build . -j$(nproc)

# Ejecutar
./ShopVibe
```

---

## 📊 Diagrama de Arquitectura

```
┌─────────────────────────────────────────────────────────────────────────┐
│                              ShopVibe                                    │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                          │
│  ┌──────────────────────────────────────────────────────────────────┐   │
│  │                         UI Layer (QML)                            │   │
│  │   Main.qml → Views → Components                                   │   │
│  └────────────────────────────┬─────────────────────────────────────┘   │
│                               │                                          │
│                               ▼                                          │
│  ┌──────────────────────────────────────────────────────────────────┐   │
│  │                    Features (Servicios QML)                       │   │
│  │  ┌────────────┐  ┌────────────┐  ┌────────────┐  ┌────────────┐  │   │
│  │  │  Currency  │──│ Inventory  │──│   Sales    │──│ Reporting  │  │   │
│  │  │  Service   │  │  Service   │  │  Service   │  │  Service   │  │   │
│  │  └─────┬──────┘  └─────┬──────┘  └─────┬──────┘  └────────────┘  │   │
│  └────────┼───────────────┼───────────────┼─────────────────────────┘   │
│           │               │               │                              │
│           └───────────────┼───────────────┘                              │
│                           ▼                                              │
│  ┌──────────────────────────────────────────────────────────────────┐   │
│  │                    Core (Shared Utilities)                        │   │
│  │  ┌────────────┐  ┌────────────┐  ┌────────────┐                  │   │
│  │  │  EventBus  │  │   Logger   │  │  Database  │                  │   │
│  │  │ (Observer) │  │            │  │  Manager   │                  │   │
│  │  └────────────┘  └────────────┘  └─────┬──────┘                  │   │
│  └────────────────────────────────────────┼─────────────────────────┘   │
│                                           │                              │
│                                           ▼                              │
│                                    ┌────────────┐                        │
│                                    │   SQLite   │                        │
│                                    └────────────┘                        │
└─────────────────────────────────────────────────────────────────────────┘
```

---

¿Quieres que continúe con la **implementación completa del Feature Currency** (tasa de cambio) con el patrón Observer funcionando? 🚀
