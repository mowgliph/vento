# Vento - Interfaz Gráfica Moderna (QML)

Crearemos una UI completa estilo Windows 11 / Fluent Design, integrando todos los servicios C++.

## 📁 Estructura de Archivos UI

```
src/ui/
├── CMakeLists.txt
└── qml/
    ├── Main.qml                    # Ventana principal
    ├── qml.qrc                     # Recursos QML
    │
    ├── components/                 # Componentes reutilizables
    │   ├── Sidebar.qml            # Navegación lateral
    │   ├── AppHeader.qml          # Encabezado superior
    │   ├── StatCard.qml           # Tarjeta de estadística
    │   ├── ProductCard.qml        # Tarjeta de producto (grid)
    │   ├── ProductListItem.qml    # Item de producto (lista)
    │   ├── CartItem.qml           # Item del carrito
    │   ├── SaleHistoryItem.qml    # Item del historial
    │   ├── FloatingActionButton.qml
    │   ├── SearchBar.qml
    │   ├── Toast.qml              # Notificaciones
    │   └── ModalDialog.qml        # Diálogos modales
    │
    ├── views/                      # Vistas principales
    │   ├── DashboardView.qml
    │   ├── SalesView.qml
    │   ├── InventoryView.qml
    │   └── SettingsView.qml
    │
    └── theme/                      # Estilos y temas
        └── Theme.qml
```

---

## 📄 src/ui/qml/theme/Theme.qml

```qml
//==============================================================================
// Theme.qml
// Vento - Sistema de diseño / Design Tokens
// Estilo: Windows 11 / Fluent Design
//==============================================================================
pragma Singleton
import QtQuick

QtObject {
    id: theme
    
    //==========================================================================
    // Colores - Paleta Principal
    //==========================================================================
    readonly property color primary: "#0078D4"           // Azul Windows 11
    readonly property color primaryLight: "#47A7F5"
    readonly property color primaryDark: "#005A9E"
    readonly property color primaryContainer: "#E6F2FB"
    
    readonly property color secondary: "#6B6B6B"
    readonly property color secondaryLight: "#9E9E9E"
    readonly property color secondaryDark: "#424242"
    
    //==========================================================================
    // Colores - Estados
    //==========================================================================
    readonly property color success: "#107C10"
    readonly property color successLight: "#DFF6DD"
    readonly property color successDark: "#0B5C0B"
    
    readonly property color warning: "#FF8C00"
    readonly property color warningLight: "#FFF4CE"
    readonly property color warningDark: "#CC7000"
    
    readonly property color error: "#D13438"
    readonly property color errorLight: "#FDE7E9"
    readonly property color errorDark: "#A52A2D"
    
    readonly property color info: "#0078D4"
    readonly property color infoLight: "#E6F2FB"
    
    //==========================================================================
    // Colores - Superficies
    //==========================================================================
    readonly property color background: "#F3F3F3"
    readonly property color surface: "#FFFFFF"
    readonly property color surfaceVariant: "#FAFAFA"
    readonly property color surfaceHover: "#F5F5F5"
    readonly property color surfacePressed: "#EBEBEB"
    
    readonly property color sidebar: "#202020"
    readonly property color sidebarHover: "#2D2D2D"
    readonly property color sidebarActive: "#0078D4"
    
    readonly property color card: "#FFFFFF"
    readonly property color cardHover: "#F8F9FA"
    readonly property color cardBorder: "#E5E5E5"
    
    //==========================================================================
    // Colores - Texto
    //==========================================================================
    readonly property color textPrimary: "#1A1A1A"
    readonly property color textSecondary: "#6B6B6B"
    readonly property color textDisabled: "#A0A0A0"
    readonly property color textOnPrimary: "#FFFFFF"
    readonly property color textOnSidebar: "#FFFFFF"
    readonly property color textLink: "#0078D4"
    
    //==========================================================================
    // Colores - Bordes y Divisores
    //==========================================================================
    readonly property color border: "#E5E5E5"
    readonly property color borderLight: "#F0F0F0"
    readonly property color borderFocus: "#0078D4"
    readonly property color divider: "#E5E5E5"
    
    //==========================================================================
    // Tipografía
    //==========================================================================
    readonly property string fontFamily: "Segoe UI, system-ui, sans-serif"
    
    readonly property int fontSizeXs: 10
    readonly property int fontSizeSm: 12
    readonly property int fontSizeMd: 14
    readonly property int fontSizeLg: 16
    readonly property int fontSizeXl: 20
    readonly property int fontSizeXxl: 24
    readonly property int fontSizeDisplay: 32
    
    readonly property int fontWeightNormal: Font.Normal
    readonly property int fontWeightMedium: Font.Medium
    readonly property int fontWeightSemiBold: Font.DemiBold
    readonly property int fontWeightBold: Font.Bold
    
    //==========================================================================
    // Espaciado
    //==========================================================================
    readonly property int spacingXs: 4
    readonly property int spacingSm: 8
    readonly property int spacingMd: 12
    readonly property int spacingLg: 16
    readonly property int spacingXl: 24
    readonly property int spacingXxl: 32
    
    //==========================================================================
    // Bordes Redondeados
    //==========================================================================
    readonly property int radiusXs: 4
    readonly property int radiusSm: 6
    readonly property int radiusMd: 8
    readonly property int radiusLg: 12
    readonly property int radiusXl: 16
    readonly property int radiusFull: 9999
    
    //==========================================================================
    // Sombras
    //==========================================================================
    readonly property color shadowColor: "#20000000"
    readonly property int shadowRadius: 8
    readonly property int shadowOffset: 2
    
    //==========================================================================
    // Animaciones
    //==========================================================================
    readonly property int animationFast: 100
    readonly property int animationNormal: 200
    readonly property int animationSlow: 300
    
    //==========================================================================
    // Dimensiones
    //==========================================================================
    readonly property int sidebarWidth: 240
    readonly property int sidebarCollapsedWidth: 60
    readonly property int headerHeight: 56
    readonly property int footerHeight: 32
    readonly property int fabSize: 56
    readonly property int iconSizeSm: 16
    readonly property int iconSizeMd: 20
    readonly property int iconSizeLg: 24
    
    //==========================================================================
    // Funciones de ayuda
    //==========================================================================
    function alpha(color, alpha) {
        return Qt.rgba(color.r, color.g, color.b, alpha)
    }
    
    function darken(color, factor) {
        return Qt.darker(color, 1 + factor)
    }
    
    function lighten(color, factor) {
        return Qt.lighter(color, 1 + factor)
    }
}
```

---

## 📄 src/ui/qml/components/Sidebar.qml

```qml
//==============================================================================
// Sidebar.qml
// Vento - Barra de navegación lateral estilo Windows 11
//==============================================================================

import QtQuick
import QtQuick.Controls
import QtQuick.Controls.Material
import QtQuick.Layouts
import "../theme"

Rectangle {
    id: sidebar
    
    //==========================================================================
    // Propiedades
    //==========================================================================
    property bool collapsed: false
    property string currentView: "dashboard"
    property alias model: navRepeater.model
    
    //==========================================================================
    // Señales
    //==========================================================================
    signal navigationRequested(string viewName)
    
    //==========================================================================
    // Dimensiones
    //==========================================================================
    width: collapsed ? Theme.sidebarCollapsedWidth : Theme.sidebarWidth
    
    Behavior on width {
        NumberAnimation { 
            duration: Theme.animationNormal
            easing.type: Easing.OutCubic
        }
    }
    
    //==========================================================================
    // Estilo
    //==========================================================================
    color: Theme.sidebar
    
    //==========================================================================
    // Contenido
    //==========================================================================
    ColumnLayout {
        anchors.fill: parent
        anchors.topMargin: Theme.spacingLg
        anchors.bottomMargin: Theme.spacingLg
        spacing: Theme.spacingXs
        
        //----------------------------------------------------------------------
        // Logo / Branding
        //----------------------------------------------------------------------
        Item {
            Layout.fillWidth: true
            Layout.preferredHeight: 60
            Layout.bottomMargin: Theme.spacingMd
            
            RowLayout {
                anchors.centerIn: parent
                spacing: Theme.spacingSm
                
                // Logo icon
                Rectangle {
                    width: 36
                    height: 36
                    radius: Theme.radiusMd
                    color: Theme.primary
                    
                    Label {
                        anchors.centerIn: parent
                        text: "🌊"
                        font.pixelSize: 20
                    }
                }
                
                // App name
                Label {
                    text: "Vento"
                    font.pixelSize: Theme.fontSizeXl
                    font.weight: Theme.fontWeightBold
                    color: Theme.textOnSidebar
                    visible: !sidebar.collapsed
                    opacity: sidebar.collapsed ? 0 : 1
                    
                    Behavior on opacity {
                        NumberAnimation { duration: Theme.animationFast }
                    }
                }
            }
        }
        
        //----------------------------------------------------------------------
        // Separador
        //----------------------------------------------------------------------
        Rectangle {
            Layout.fillWidth: true
            Layout.leftMargin: Theme.spacingMd
            Layout.rightMargin: Theme.spacingMd
            height: 1
            color: Theme.alpha(Theme.textOnSidebar, 0.1)
        }
        
        //----------------------------------------------------------------------
        // Items de navegación
        //----------------------------------------------------------------------
        Repeater {
            id: navRepeater
            
            model: [
                { 
                    id: "dashboard", 
                    icon: "📊", 
                    text: "Dashboard",
                    badge: 0
                },
                { 
                    id: "sales", 
                    icon: "🛒", 
                    text: "Ventas",
                    badge: 0
                },
                { 
                    id: "inventory", 
                    icon: "📦", 
                    text: "Inventario",
                    badge: 0
                },
                { 
                    id: "settings", 
                    icon: "⚙️", 
                    text: "Configuración",
                    badge: 0
                }
            ]
            
            delegate: SidebarItem {
                Layout.fillWidth: true
                Layout.leftMargin: Theme.spacingSm
                Layout.rightMargin: Theme.spacingSm
                
                icon: modelData.icon
                text: modelData.text
                badge: modelData.badge
                isActive: currentView === modelData.id
                isCollapsed: sidebar.collapsed
                
                onClicked: {
                    currentView = modelData.id
                    navigationRequested(modelData.id)
                }
            }
        }
        
        //----------------------------------------------------------------------
        // Espaciador
        //----------------------------------------------------------------------
        Item { Layout.fillHeight: true }
        
        //----------------------------------------------------------------------
        // Separador inferior
        //----------------------------------------------------------------------
        Rectangle {
            Layout.fillWidth: true
            Layout.leftMargin: Theme.spacingMd
            Layout.rightMargin: Theme.spacingMd
            height: 1
            color: Theme.alpha(Theme.textOnSidebar, 0.1)
        }
        
        //----------------------------------------------------------------------
        // Información de versión
        //----------------------------------------------------------------------
        Label {
            Layout.alignment: Qt.AlignHCenter
            text: sidebar.collapsed ? "v1" : "Vento v" + (typeof appVersion !== 'undefined' ? appVersion : "1.0")
            font.pixelSize: Theme.fontSizeXs
            color: Theme.alpha(Theme.textOnSidebar, 0.5)
        }
    }
}

//==============================================================================
// Componente: Item de navegación
//==============================================================================
component SidebarItem: ItemDelegate {
    id: sidebarItem
    
    required property string icon
    required property string text
    property int badge: 0
    property bool isActive: false
    property bool isCollapsed: false
    
    height: 48
    
    background: Rectangle {
        radius: Theme.radiusMd
        color: {
            if (sidebarItem.isActive) return Theme.sidebarActive
            if (sidebarItem.hovered) return Theme.sidebarHover
            return "transparent"
        }
        
        Behavior on color {
            ColorAnimation { duration: Theme.animationFast }
        }
    }
    
    contentItem: RowLayout {
        spacing: sidebarItem.isCollapsed ? 0 : Theme.spacingMd
        
        // Indicador activo
        Rectangle {
            width: 3
            height: 24
            radius: 2
            color: sidebarItem.isActive ? Theme.textOnSidebar : "transparent"
            opacity: sidebarItem.isActive ? 1 : 0
            
            Behavior on opacity {
                NumberAnimation { duration: Theme.animationFast }
            }
        }
        
        // Icono
        Label {
            text: sidebarItem.icon
            font.pixelSize: Theme.fontSizeXl
            Layout.leftMargin: sidebarItem.isCollapsed ? Theme.spacingMd : 0
        }
        
        // Texto
        Label {
            text: sidebarItem.text
            color: Theme.textOnSidebar
            font.pixelSize: Theme.fontSizeMd
            font.weight: sidebarItem.isActive ? Theme.fontWeightMedium : Theme.fontWeightNormal
            visible: !sidebarItem.isCollapsed
            opacity: sidebarItem.isCollapsed ? 0 : 1
            Layout.fillWidth: true
            
            Behavior on opacity {
                NumberAnimation { duration: Theme.animationFast }
            }
        }
        
        // Badge
        Rectangle {
            visible: sidebarItem.badge > 0 && !sidebarItem.isCollapsed
            width: 20
            height: 20
            radius: 10
            color: Theme.error
            
            Label {
                anchors.centerIn: parent
                text: sidebarItem.badge > 99 ? "99+" : sidebarItem.badge
                font.pixelSize: Theme.fontSizeXs
                font.weight: Theme.fontWeightBold
                color: Theme.textOnPrimary
            }
        }
    }
    
    ToolTip.visible: sidebarItem.isCollapsed && hovered
    ToolTip.text: sidebarItem.text
    ToolTip.delay: 500
}
```

---

## 📄 src/ui/qml/components/AppHeader.qml

```qml
//==============================================================================
// AppHeader.qml
// Vento - Encabezado superior de la aplicación
//==============================================================================

import QtQuick
import QtQuick.Controls
import QtQuick.Controls.Material
import QtQuick.Layouts
import Vento.Currency 1.0
import "../theme"

Rectangle {
    id: header
    
    //==========================================================================
    // Propiedades
    //==========================================================================
    property bool sidebarCollapsed: false
    property string searchPlaceholder: "Buscar..."
    
    //==========================================================================
    // Señales
    //==========================================================================
    signal menuClicked()
    signal searchTextChanged(string text)
    signal notificationsClicked()
    signal settingsClicked()
    
    //==========================================================================
    // Dimensiones
    //==========================================================================
    height: Theme.headerHeight
    
    //==========================================================================
    // Estilo
    //==========================================================================
    color: Theme.surface
    
    // Sombra inferior
    Rectangle {
        anchors.bottom: parent.bottom
        width: parent.width
        height: 1
        color: Theme.border
    }
    
    //==========================================================================
    // Contenido
    //==========================================================================
    RowLayout {
        anchors.fill: parent
        anchors.leftMargin: Theme.spacingLg
        anchors.rightMargin: Theme.spacingLg
        spacing: Theme.spacingLg
        
        //----------------------------------------------------------------------
        // Botón de menú
        //----------------------------------------------------------------------
        ToolButton {
            id: menuButton
            icon.source: "qrc:/icons/menu.svg"
            icon.width: Theme.iconSizeMd
            icon.height: Theme.iconSizeMd
            
            onClicked: header.menuClicked()
            
            ToolTip.visible: hovered
            ToolTip.text: sidebarCollapsed ? "Expandir menú" : "Contraer menú"
            
            // Si no hay ícono, usar texto
            Label {
                anchors.centerIn: parent
                text: "☰"
                font.pixelSize: Theme.fontSizeLg
                visible: menuButton.icon.source.toString() === ""
            }
        }
        
        //----------------------------------------------------------------------
        // Barra de búsqueda
        //----------------------------------------------------------------------
        SearchBar {
            Layout.preferredWidth: 400
            placeholder: searchPlaceholder
            
            onTextChanged: (text) => header.searchTextChanged(text)
        }
        
        //----------------------------------------------------------------------
        // Espaciador
        //----------------------------------------------------------------------
        Item { Layout.fillWidth: true }
        
        //----------------------------------------------------------------------
        // Indicador de tasa de cambio
        //----------------------------------------------------------------------
        Pane {
            Layout.preferredHeight: 40
            padding: Theme.spacingSm
            
            Material.elevation: 0
            Material.background: Theme.primaryContainer
            
            background: Rectangle {
                radius: Theme.radiusMd
                color: Theme.primaryContainer
            }
            
            RowLayout {
                anchors.centerIn: parent
                spacing: Theme.spacingSm
                
                Label {
                    text: "💵"
                    font.pixelSize: Theme.fontSizeLg
                }
                
                Label {
                    text: "USD:"
                    color: Theme.textSecondary
                    font.pixelSize: Theme.fontSizeSm
                }
                
                Label {
                    text: CurrencyService.localCurrencySymbol + " " + 
                          CurrencyService.exchangeRateFormatted
                    color: Theme.primary
                    font.weight: Theme.fontWeightBold
                    font.pixelSize: Theme.fontSizeMd
                }
                
                // Indicador de tendencia
                Label {
                    text: {
                        switch(CurrencyService.trend) {
                            case "up": return "↑"
                            case "down": return "↓"
                            default: return ""
                        }
                    }
                    color: CurrencyService.trend === "up" ? Theme.error : Theme.success
                    font.weight: Theme.fontWeightBold
                    visible: CurrencyService.trend !== "stable"
                }
            }
        }
        
        //----------------------------------------------------------------------
        // Notificaciones
        //----------------------------------------------------------------------
        ToolButton {
            icon.source: "qrc:/icons/notifications.svg"
            icon.width: Theme.iconSizeMd
            icon.height: Theme.iconSizeMd
            
            onClicked: header.notificationsClicked()
            
            Label {
                anchors.centerIn: parent
                text: "🔔"
                font.pixelSize: Theme.fontSizeLg
                visible: parent.icon.source.toString() === ""
            }
            
            // Badge de notificaciones
            Rectangle {
                anchors.top: parent.top
                anchors.right: parent.right
                anchors.margins: 8
                width: 8
                height: 8
                radius: 4
                color: Theme.error
                visible: false // Cambiar a true cuando haya notificaciones
            }
        }
        
        //----------------------------------------------------------------------
        // Configuración rápida
        //----------------------------------------------------------------------
        ToolButton {
            icon.source: "qrc:/icons/settings.svg"
            icon.width: Theme.iconSizeMd
            icon.height: Theme.iconSizeMd
            
            onClicked: header.settingsClicked()
            
            Label {
                anchors.centerIn: parent
                text: "⚙️"
                font.pixelSize: Theme.fontSizeLg
                visible: parent.icon.source.toString() === ""
            }
        }
    }
}
```

---

## 📄 src/ui/qml/components/SearchBar.qml

```qml
//==============================================================================
// SearchBar.qml
// Vento - Barra de búsqueda reutilizable
//==============================================================================

import QtQuick
import QtQuick.Controls
import QtQuick.Controls.Material
import QtQuick.Layouts
import "../theme"

TextField {
    id: searchBar
    
    //==========================================================================
    // Propiedades
    //==========================================================================
    property string placeholder: "Buscar..."
    property int debounceMs: 300
    
    //==========================================================================
    // Señales
    //==========================================================================
    signal searchTriggered(string text)
    
    //==========================================================================
    // Configuración
    //==========================================================================
    placeholderText: placeholder
    
    leftPadding: 40
    rightPadding: text.length > 0 ? 40 : 16
    
    //==========================================================================
    // Debounce Timer
    //==========================================================================
    Timer {
        id: debounceTimer
        interval: searchBar.debounceMs
        onTriggered: searchBar.searchTriggered(searchBar.text)
    }
    
    onTextChanged: debounceTimer.restart()
    
    //==========================================================================
    // Estilo
    //==========================================================================
    background: Rectangle {
        radius: Theme.radiusMd
        color: Theme.background
        border.color: searchBar.activeFocus ? Theme.borderFocus : "transparent"
        border.width: 2
        
        Behavior on border.color {
            ColorAnimation { duration: Theme.animationFast }
        }
    }
    
    //==========================================================================
    // Ícono de búsqueda
    //==========================================================================
    Label {
        anchors.left: parent.left
        anchors.leftMargin: Theme.spacingMd
        anchors.verticalCenter: parent.verticalCenter
        text: "🔍"
        font.pixelSize: Theme.fontSizeMd
        color: Theme.textSecondary
    }
    
    //==========================================================================
    // Botón de limpiar
    //==========================================================================
    ToolButton {
        anchors.right: parent.right
        anchors.rightMargin: Theme.spacingXs
        anchors.verticalCenter: parent.verticalCenter
        width: 32
        height: 32
        visible: searchBar.text.length > 0
        
        onClicked: {
            searchBar.text = ""
            searchBar.forceActiveFocus()
        }
        
        Label {
            anchors.centerIn: parent
            text: "✕"
            font.pixelSize: Theme.fontSizeSm
            color: Theme.textSecondary
        }
    }
}
```

---

## 📄 src/ui/qml/components/StatCard.qml

```qml
//==============================================================================
// StatCard.qml
// Vento - Tarjeta de estadística para dashboard
//==============================================================================

import QtQuick
import QtQuick.Controls
import QtQuick.Controls.Material
import QtQuick.Layouts
import "../theme"

Pane {
    id: statCard
    
    //==========================================================================
    // Propiedades
    //==========================================================================
    property string icon: "📊"
    property string title: "Estadística"
    property string value: "0"
    property string subtitle: ""
    property color accentColor: Theme.primary
    property string trend: "" // "up", "down", ""
    property string trendValue: ""
    
    //==========================================================================
    // Dimensiones
    //==========================================================================
    implicitWidth: 200
    implicitHeight: 120
    
    //==========================================================================
    // Estilo
    //==========================================================================
    Material.elevation: 1
    Material.background: Theme.card
    
    padding: Theme.spacingLg
    
    background: Rectangle {
        radius: Theme.radiusLg
        color: Theme.card
        border.color: Theme.cardBorder
        border.width: 1
        
        // Línea de acento superior
        Rectangle {
            anchors.top: parent.top
            anchors.left: parent.left
            anchors.right: parent.right
            height: 4
            radius: Theme.radiusLg
            color: statCard.accentColor
            
            // Solo redondear esquinas superiores
            Rectangle {
                anchors.bottom: parent.bottom
                anchors.left: parent.left
                anchors.right: parent.right
                height: 2
                color: statCard.accentColor
            }
        }
    }
    
    //==========================================================================
    // Contenido
    //==========================================================================
    ColumnLayout {
        anchors.fill: parent
        spacing: Theme.spacingSm
        
        // Header con ícono y título
        RowLayout {
            Layout.fillWidth: true
            spacing: Theme.spacingSm
            
            Label {
                text: statCard.icon
                font.pixelSize: Theme.fontSizeLg
            }
            
            Label {
                text: statCard.title
                font.pixelSize: Theme.fontSizeSm
                color: Theme.textSecondary
                elide: Text.ElideRight
                Layout.fillWidth: true
            }
        }
        
        // Valor principal
        Label {
            text: statCard.value
            font.pixelSize: Theme.fontSizeDisplay
            font.weight: Theme.fontWeightBold
            color: Theme.textPrimary
            Layout.fillWidth: true
        }
        
        // Subtítulo y tendencia
        RowLayout {
            Layout.fillWidth: true
            spacing: Theme.spacingSm
            
            Label {
                text: statCard.subtitle
                font.pixelSize: Theme.fontSizeSm
                color: Theme.textSecondary
                visible: statCard.subtitle !== ""
                Layout.fillWidth: true
            }
            
            // Indicador de tendencia
            Row {
                spacing: 4
                visible: statCard.trend !== ""
                
                Label {
                    text: statCard.trend === "up" ? "↑" : "↓"
                    font.weight: Theme.fontWeightBold
                    color: statCard.trend === "up" ? Theme.success : Theme.error
                }
                
                Label {
                    text: statCard.trendValue
                    font.pixelSize: Theme.fontSizeSm
                    font.weight: Theme.fontWeightMedium
                    color: statCard.trend === "up" ? Theme.success : Theme.error
                }
            }
        }
    }
}
```

---

## 📄 src/ui/qml/components/ProductCard.qml

```qml
//==============================================================================
// ProductCard.qml
// Vento - Tarjeta de producto para grid de ventas
//==============================================================================

import QtQuick
import QtQuick.Controls
import QtQuick.Controls.Material
import QtQuick.Layouts
import "../theme"

ItemDelegate {
    id: productCard
    
    //==========================================================================
    // Propiedades
    //==========================================================================
    property string productName: "Producto"
    property string category: ""
    property string formattedPrice: "Bs. 0.00"
    property int stockQuantity: 0
    property string stockStatus: "ok" // "ok", "low", "out"
    
    //==========================================================================
    // Dimensiones
    //==========================================================================
    implicitWidth: 160
    implicitHeight: 150
    
    //==========================================================================
    // Estado
    //==========================================================================
    enabled: stockQuantity > 0
    opacity: enabled ? 1.0 : 0.5
    
    //==========================================================================
    // Estilo
    //==========================================================================
    padding: Theme.spacingMd
    
    background: Rectangle {
        radius: Theme.radiusLg
        color: productCard.hovered ? Theme.cardHover : Theme.card
        border.color: productCard.hovered ? Theme.primary : Theme.cardBorder
        border.width: productCard.hovered ? 2 : 1
        
        Behavior on color {
            ColorAnimation { duration: Theme.animationFast }
        }
        
        Behavior on border.color {
            ColorAnimation { duration: Theme.animationFast }
        }
        
        // Indicador de estado de stock
        Rectangle {
            anchors.top: parent.top
            anchors.left: parent.left
            anchors.right: parent.right
            height: 4
            radius: Theme.radiusLg
            color: {
                switch(productCard.stockStatus) {
                    case "out": return Theme.error
                    case "low": return Theme.warning
                    default: return Theme.success
                }
            }
            
            Rectangle {
                anchors.bottom: parent.bottom
                anchors.left: parent.left
                anchors.right: parent.right
                height: 2
                color: parent.color
            }
        }
    }
    
    //==========================================================================
    // Contenido
    //==========================================================================
    contentItem: ColumnLayout {
        spacing: Theme.spacingXs
        
        // Nombre del producto
        Label {
            text: productCard.productName
            font.pixelSize: Theme.fontSizeMd
            font.weight: Theme.fontWeightMedium
            color: Theme.textPrimary
            elide: Text.ElideRight
            wrapMode: Text.WordWrap
            maximumLineCount: 2
            Layout.fillWidth: true
            horizontalAlignment: Text.AlignHCenter
        }
        
        // Categoría
        Label {
            text: productCard.category
            font.pixelSize: Theme.fontSizeXs
            color: Theme.textSecondary
            visible: productCard.category !== ""
            Layout.alignment: Qt.AlignHCenter
        }
        
        Item { Layout.fillHeight: true }
        
        // Precio
        Label {
            text: productCard.formattedPrice
            font.pixelSize: Theme.fontSizeXl
            font.weight: Theme.fontWeightBold
            color: Theme.primary
            Layout.alignment: Qt.AlignHCenter
        }
        
        // Stock
        Rectangle {
            Layout.alignment: Qt.AlignHCenter
            width: stockLabel.width + Theme.spacingMd * 2
            height: 24
            radius: 12
            color: {
                switch(productCard.stockStatus) {
                    case "out": return Theme.errorLight
                    case "low": return Theme.warningLight
                    default: return Theme.successLight
                }
            }
            
            Label {
                id: stockLabel
                anchors.centerIn: parent
                text: productCard.stockQuantity > 0 ? 
                      "Stock: " + productCard.stockQuantity : "Agotado"
                font.pixelSize: Theme.fontSizeXs
                font.weight: Theme.fontWeightMedium
                color: {
                    switch(productCard.stockStatus) {
                        case "out": return Theme.error
                        case "low": return Theme.warning
                        default: return Theme.success
                    }
                }
            }
        }
    }
}
```

---

## 📄 src/ui/qml/components/FloatingActionButton.qml

```qml
//==============================================================================
// FloatingActionButton.qml
// Vento - Botón de acción flotante (FAB)
//==============================================================================

import QtQuick
import QtQuick.Controls
import QtQuick.Controls.Material
import "../theme"

RoundButton {
    id: fab
    
    //==========================================================================
    // Propiedades
    //==========================================================================
    property string iconText: "+"
    property bool extended: false
    property string extendedText: ""
    
    //==========================================================================
    // Dimensiones
    //==========================================================================
    width: extended ? implicitWidth : Theme.fabSize
    height: Theme.fabSize
    radius: extended ? Theme.radiusLg : Theme.fabSize / 2
    
    //==========================================================================
    // Estilo
    //==========================================================================
    Material.elevation: hovered ? 8 : 4
    Material.background: Theme.primary
    
    contentItem: Row {
        spacing: Theme.spacingSm
        anchors.centerIn: parent
        
        Label {
            text: fab.iconText
            font.pixelSize: Theme.fontSizeXl
            font.weight: Theme.fontWeightBold
            color: Theme.textOnPrimary
            anchors.verticalCenter: parent.verticalCenter
        }
        
        Label {
            text: fab.extendedText
            font.pixelSize: Theme.fontSizeMd
            font.weight: Theme.fontWeightMedium
            color: Theme.textOnPrimary
            visible: fab.extended
            anchors.verticalCenter: parent.verticalCenter
        }
    }
    
    //==========================================================================
    // Sombra
    //==========================================================================
    background: Rectangle {
        radius: fab.radius
        color: fab.pressed ? Theme.primaryDark : Theme.primary
        
        Behavior on color {
            ColorAnimation { duration: Theme.animationFast }
        }
        
        // Sombra simulada
        Rectangle {
            anchors.fill: parent
            anchors.topMargin: 2
            radius: parent.radius
            color: Theme.alpha(Qt.black, 0.2)
            z: -1
        }
    }
}
```

---

## 📄 src/ui/qml/components/Toast.qml

```qml
//==============================================================================
// Toast.qml
// Vento - Notificaciones toast
//==============================================================================

import QtQuick
import QtQuick.Controls
import QtQuick.Layouts
import "../theme"

Rectangle {
    id: toast
    
    //==========================================================================
    // Propiedades
    //==========================================================================
    property string message: ""
    property string type: "info" // "info", "success", "warning", "error"
    property int duration: 3000
    property bool autoHide: true
    
    //==========================================================================
    // Métodos públicos
    //==========================================================================
    function show(msg, toastType = "info", dur = 3000) {
        message = msg
        type = toastType
        duration = dur
        state = "visible"
        
        if (autoHide) {
            hideTimer.restart()
        }
    }
    
    function hide() {
        state = "hidden"
    }
    
    //==========================================================================
    // Timer para auto-hide
    //==========================================================================
    Timer {
        id: hideTimer
        interval: toast.duration
        onTriggered: toast.hide()
    }
    
    //==========================================================================
    // Estilo
    //==========================================================================
    width: contentRow.width + Theme.spacingXl * 2
    height: 48
    radius: Theme.radiusMd
    
    color: {
        switch(type) {
            case "success": return Theme.success
            case "warning": return Theme.warning
            case "error": return Theme.error
            default: return Theme.primary
        }
    }
    
    //==========================================================================
    // Estados y transiciones
    //==========================================================================
    state: "hidden"
    
    states: [
        State {
            name: "hidden"
            PropertyChanges { target: toast; opacity: 0; y: -toast.height }
        },
        State {
            name: "visible"
            PropertyChanges { target: toast; opacity: 1; y: Theme.spacingMd }
        }
    ]
    
    transitions: [
        Transition {
            from: "hidden"; to: "visible"
            ParallelAnimation {
                NumberAnimation { property: "opacity"; duration: Theme.animationNormal }
                NumberAnimation { property: "y"; duration: Theme.animationNormal; easing.type: Easing.OutBack }
            }
        },
        Transition {
            from: "visible"; to: "hidden"
            ParallelAnimation {
                NumberAnimation { property: "opacity"; duration: Theme.animationFast }
                NumberAnimation { property: "y"; duration: Theme.animationFast }
            }
        }
    ]
    
    //==========================================================================
    // Contenido
    //==========================================================================
    Row {
        id: contentRow
        anchors.centerIn: parent
        spacing: Theme.spacingSm
        
        Label {
            text: {
                switch(toast.type) {
                    case "success": return "✓"
                    case "warning": return "⚠"
                    case "error": return "✕"
                    default: return "ℹ"
                }
            }
            font.pixelSize: Theme.fontSizeLg
            color: Theme.textOnPrimary
            anchors.verticalCenter: parent.verticalCenter
        }
        
        Label {
            text: toast.message
            font.pixelSize: Theme.fontSizeMd
            color: Theme.textOnPrimary
            anchors.verticalCenter: parent.verticalCenter
        }
    }
    
    //==========================================================================
    // Botón de cerrar
    //==========================================================================
    MouseArea {
        anchors.fill: parent
        onClicked: toast.hide()
        cursorShape: Qt.PointingHandCursor
    }
}
```

---

## 📄 src/ui/qml/views/DashboardView.qml

```qml
//==============================================================================
// DashboardView.qml
// Vento - Vista de Dashboard / Panel principal
//==============================================================================

import QtQuick
import QtQuick.Controls
import QtQuick.Controls.Material
import QtQuick.Layouts

import Vento.Currency 1.0
import Vento.Inventory 1.0
import Vento.Sales 1.0

import "../theme"
import "../components"

ScrollView {
    id: dashboardView
    
    contentWidth: availableWidth
    
    ColumnLayout {
        width: parent.width
        spacing: Theme.spacingXl
        
        //======================================================================
        // Encabezado de bienvenida
        //======================================================================
        RowLayout {
            Layout.fillWidth: true
            spacing: Theme.spacingLg
            
            ColumnLayout {
                Layout.fillWidth: true
                spacing: Theme.spacingXs
                
                Label {
                    text: "¡Bienvenido a Vento!"
                    font.pixelSize: Theme.fontSizeDisplay
                    font.weight: Theme.fontWeightBold
                    color: Theme.textPrimary
                }
                
                Label {
                    text: Qt.formatDate(new Date(), "dddd, d 'de' MMMM yyyy")
                    font.pixelSize: Theme.fontSizeLg
                    color: Theme.textSecondary
                }
            }
            
            // Hora actual
            Label {
                text: Qt.formatTime(new Date(), "hh:mm")
                font.pixelSize: Theme.fontSizeDisplay
                font.weight: Theme.fontWeightLight
                color: Theme.textSecondary
                
                Timer {
                    interval: 1000
                    running: true
                    repeat: true
                    onTriggered: parent.text = Qt.formatTime(new Date(), "hh:mm")
                }
            }
        }
        
        //======================================================================
        // Tarjetas de resumen
        //======================================================================
        Label {
            text: "📈 Resumen del Día"
            font.pixelSize: Theme.fontSizeXl
            font.weight: Theme.fontWeightSemiBold
            color: Theme.textPrimary
        }
        
        GridLayout {
            Layout.fillWidth: true
            columns: 4
            columnSpacing: Theme.spacingLg
            rowSpacing: Theme.spacingLg
            
            StatCard {
                Layout.fillWidth: true
                icon: "💰"
                title: "Ventas Hoy"
                value: CurrencyService.formatLocal(SalesService.todayTotal)
                subtitle: SalesService.todayCount + " transacciones"
                accentColor: Theme.success
            }
            
            StatCard {
                Layout.fillWidth: true
                icon: "📊"
                title: "Ventas Semana"
                value: CurrencyService.formatLocal(SalesService.weekTotal)
                accentColor: Theme.primary
            }
            
            StatCard {
                Layout.fillWidth: true
                icon: "📦"
                title: "Productos"
                value: InventoryService.totalProducts.toString()
                subtitle: InventoryService.lowStockCount + " con bajo stock"
                accentColor: Theme.info
            }
            
            StatCard {
                Layout.fillWidth: true
                icon: "💱"
                title: "Tasa USD"
                value: CurrencyService.localCurrencySymbol + " " + CurrencyService.exchangeRateFormatted
                subtitle: CurrencyService.lastUpdateFormatted
                accentColor: Theme.warning
                trend: CurrencyService.trend
                trendValue: Math.abs(CurrencyService.changePercent).toFixed(2) + "%"
            }
        }
        
        //======================================================================
        // Sección de alertas
        //======================================================================
        Pane {
            Layout.fillWidth: true
            visible: InventoryService.lowStockCount > 0 || InventoryService.outOfStockCount > 0
            
            Material.background: Theme.warningLight
            
            background: Rectangle {
                radius: Theme.radiusMd
                color: Theme.warningLight
                border.color: Theme.warning
                border.width: 1
            }
            
            RowLayout {
                anchors.fill: parent
                spacing: Theme.spacingMd
                
                Label {
                    text: "⚠️"
                    font.pixelSize: Theme.fontSizeXl
                }
                
                ColumnLayout {
                    Layout.fillWidth: true
                    spacing: 2
                    
                    Label {
                        text: "Alertas de Inventario"
                        font.weight: Theme.fontWeightSemiBold
                        color: Theme.warningDark
                    }
                    
                    Label {
                        text: {
                            let msg = ""
                            if (InventoryService.lowStockCount > 0) {
                                msg += InventoryService.lowStockCount + " productos con bajo stock"
                            }
                            if (InventoryService.outOfStockCount > 0) {
                                if (msg) msg += " · "
                                msg += InventoryService.outOfStockCount + " productos agotados"
                            }
                            return msg
                        }
                        font.pixelSize: Theme.fontSizeSm
                        color: Theme.warningDark
                    }
                }
                
                Button {
                    text: "Ver Inventario"
                    Material.background: Theme.warning
                    Material.foreground: Theme.textOnPrimary
                    
                    onClicked: {
                        // Navegar a inventario
                    }
                }
            }
        }
        
        //======================================================================
        // Accesos rápidos
        //======================================================================
        Label {
            text: "⚡ Acciones Rápidas"
            font.pixelSize: Theme.fontSizeXl
            font.weight: Theme.fontWeightSemiBold
            color: Theme.textPrimary
        }
        
        RowLayout {
            Layout.fillWidth: true
            spacing: Theme.spacingLg
            
            Repeater {
                model: [
                    { icon: "🛒", text: "Nueva Venta", color: Theme.success, action: "sales" },
                    { icon: "📦", text: "Agregar Producto", color: Theme.primary, action: "addProduct" },
                    { icon: "💱", text: "Actualizar Tasa", color: Theme.warning, action: "updateRate" },
                    { icon: "📊", text: "Ver Reportes", color: Theme.info, action: "reports" }
                ]
                
                delegate: Pane {
                    Layout.fillWidth: true
                    Layout.preferredHeight: 100
                    
                    Material.background: Theme.card
                    
                    background: Rectangle {
                        radius: Theme.radiusLg
                        color: hoverHandler.hovered ? modelData.color : Theme.card
                        border.color: modelData.color
                        border.width: 2
                        
                        Behavior on color {
                            ColorAnimation { duration: Theme.animationNormal }
                        }
                        
                        HoverHandler { id: hoverHandler }
                    }
                    
                    ColumnLayout {
                        anchors.centerIn: parent
                        spacing: Theme.spacingSm
                        
                        Label {
                            text: modelData.icon
                            font.pixelSize: 32
                            Layout.alignment: Qt.AlignHCenter
                        }
                        
                        Label {
                            text: modelData.text
                            font.weight: Theme.fontWeightMedium
                            color: hoverHandler.hovered ? Theme.textOnPrimary : Theme.textPrimary
                            Layout.alignment: Qt.AlignHCenter
                        }
                    }
                    
                    MouseArea {
                        anchors.fill: parent
                        cursorShape: Qt.PointingHandCursor
                        onClicked: {
                            switch(modelData.action) {
                                case "sales":
                                    // Navegar a ventas
                                    break
                                case "addProduct":
                                    addProductDialog.open()
                                    break
                                case "updateRate":
                                    updateRateDialog.open()
                                    break
                            }
                        }
                    }
                }
            }
        }
        
        //======================================================================
        // Últimas ventas
        //======================================================================
        Label {
            text: "🕐 Últimas Ventas"
            font.pixelSize: Theme.fontSizeXl
            font.weight: Theme.fontWeightSemiBold
            color: Theme.textPrimary
        }
        
        Pane {
            Layout.fillWidth: true
            Material.elevation: 1
            Material.background: Theme.card
            
            padding: 0
            
            ListView {
                id: recentSalesList
                width: parent.width
                height: Math.min(contentHeight, 300)
                clip: true
                
                model: SalesService.salesHistory
                
                delegate: ItemDelegate {
                    width: recentSalesList.width
                    height: 60
                    
                    RowLayout {
                        anchors.fill: parent
                        anchors.leftMargin: Theme.spacingLg
                        anchors.rightMargin: Theme.spacingLg
                        spacing: Theme.spacingMd
                        
                        Rectangle {
                            width: 40
                            height: 40
                            radius: 20
                            color: Theme.primaryContainer
                            
                            Label {
                                anchors.centerIn: parent
                                text: "🛒"
                                font.pixelSize: Theme.fontSizeLg
                            }
                        }
                        
                        ColumnLayout {
                            Layout.fillWidth: true
                            spacing: 2
                            
                            Label {
                                text: "Venta #" + model.id
                                font.weight: Theme.fontWeightMedium
                            }
                            
                            Label {
                                text: model.formattedDate + " " + model.formattedTime + " · " + 
                                      model.itemCount + " items"
                                font.pixelSize: Theme.fontSizeSm
                                color: Theme.textSecondary
                            }
                        }
                        
                        Label {
                            text: model.paymentMethodDisplay
                            font.pixelSize: Theme.fontSizeSm
                            color: Theme.textSecondary
                        }
                        
                        Label {
                            text: model.formattedTotal
                            font.pixelSize: Theme.fontSizeLg
                            font.weight: Theme.fontWeightBold
                            color: Theme.success
                        }
                    }
                }
                
                // Empty state
                Label {
                    anchors.centerIn: parent
                    text: "No hay ventas registradas hoy"
                    color: Theme.textSecondary
                    visible: recentSalesList.count === 0
                }
            }
        }
        
        // Espaciado inferior
        Item { Layout.preferredHeight: Theme.spacingXl }
    }
    
    //==========================================================================
    // Diálogos
    //==========================================================================
    
    // Diálogo para actualizar tasa
    Dialog {
        id: updateRateDialog
        title: "Actualizar Tasa de Cambio"
        modal: true
        anchors.centerIn: parent
        width: 400
        
        standardButtons: Dialog.Ok | Dialog.Cancel
        
        onAccepted: {
            let rate = parseFloat(rateInput.text.replace(",", "."))
            if (!isNaN(rate) && rate > 0) {
                CurrencyService.updateRate(rate)
            }
        }
        
        ColumnLayout {
            anchors.fill: parent
            spacing: Theme.spacingLg
            
            Label {
                text: "Tasa actual: " + CurrencyService.localCurrencySymbol + " " + 
                      CurrencyService.exchangeRateFormatted
                color: Theme.textSecondary
            }
            
            TextField {
                id: rateInput
                Layout.fillWidth: true
                placeholderText: "Nueva tasa"
                text: CurrencyService.exchangeRateFormatted
                validator: DoubleValidator { bottom: 0.01; decimals: 4 }
                
                Label {
                    anchors.right: parent.right
                    anchors.rightMargin: Theme.spacingMd
                    anchors.verticalCenter: parent.verticalCenter
                    text: CurrencyService.localCurrencySymbol + " por $1 USD"
                    color: Theme.textSecondary
                    font.pixelSize: Theme.fontSizeSm
                }
            }
        }
    }
}
```

---

## 📄 src/ui/qml/views/InventoryView.qml

```qml
//==============================================================================
// InventoryView.qml
// Vento - Vista de gestión de inventario
//==============================================================================

import QtQuick
import QtQuick.Controls
import QtQuick.Controls.Material
import QtQuick.Layouts

import Vento.Inventory 1.0
import Vento.Currency 1.0

import "../theme"
import "../components"

Item {
    id: inventoryView
    
    //==========================================================================
    // Layout principal
    //==========================================================================
    ColumnLayout {
        anchors.fill: parent
        spacing: Theme.spacingLg
        
        //----------------------------------------------------------------------
        // Header con estadísticas
        //----------------------------------------------------------------------
        RowLayout {
            Layout.fillWidth: true
            spacing: Theme.spacingMd
            
            Label {
                text: "📦 Inventario"
                font.pixelSize: Theme.fontSizeDisplay
                font.weight: Theme.fontWeightBold
                color: Theme.textPrimary
            }
            
            Item { Layout.fillWidth: true }
            
            // Mini stats
            Repeater {
                model: [
                    { 
                        label: "Total", 
                        value: InventoryService.totalProducts,
                        color: Theme.primary
                    },
                    { 
                        label: "Bajo Stock", 
                        value: InventoryService.lowStockCount,
                        color: Theme.warning
                    },
                    { 
                        label: "Agotados", 
                        value: InventoryService.outOfStockCount,
                        color: Theme.error
                    }
                ]
                
                delegate: Pane {
                    Layout.preferredWidth: 120
                    padding: Theme.spacingSm
                    
                    Material.background: Theme.alpha(modelData.color, 0.1)
                    
                    background: Rectangle {
                        radius: Theme.radiusMd
                        color: Theme.alpha(modelData.color, 0.1)
                    }
                    
                    RowLayout {
                        anchors.centerIn: parent
                        spacing: Theme.spacingSm
                        
                        Label {
                            text: modelData.value
                            font.pixelSize: Theme.fontSizeXl
                            font.weight: Theme.fontWeightBold
                            color: modelData.color
                        }
                        
                        Label {
                            text: modelData.label
                            font.pixelSize: Theme.fontSizeSm
                            color: Theme.textSecondary
                        }
                    }
                }
            }
        }
        
        //----------------------------------------------------------------------
        // Barra de búsqueda y filtros
        //----------------------------------------------------------------------
        Pane {
            Layout.fillWidth: true
            Material.elevation: 1
            Material.background: Theme.card
            
            RowLayout {
                anchors.fill: parent
                spacing: Theme.spacingMd
                
                SearchBar {
                    Layout.preferredWidth: 350
                    placeholder: "Buscar productos..."
                    
                    onSearchTriggered: (text) => {
                        if (text.length >= 2) {
                            InventoryService.products.search(text)
                        } else if (text.length === 0) {
                            InventoryService.products.load()
                        }
                    }
                }
                
                ComboBox {
                    id: categoryFilter
                    Layout.preferredWidth: 200
                    model: ["Todas las categorías"].concat(InventoryService.categories)
                    
                    onCurrentTextChanged: {
                        if (currentIndex === 0) {
                            InventoryService.products.setCategoryFilter("")
                        } else {
                            InventoryService.products.setCategoryFilter(currentText)
                        }
                    }
                }
                
                ComboBox {
                    id: sortCombo
                    Layout.preferredWidth: 180
                    model: ["Nombre A-Z", "Nombre Z-A", "Precio ↑", "Precio ↓", "Stock ↑", "Stock ↓"]
                    
                    onCurrentIndexChanged: {
                        let configs = [
                            ["name", true], ["name", false],
                            ["price", true], ["price", false],
                            ["stock", true], ["stock", false]
                        ]
                        let [field, asc] = configs[currentIndex]
                        InventoryService.products.sortBy(field, asc)
                    }
                }
                
                Item { Layout.fillWidth: true }
                
                Label {
                    text: InventoryService.products.count + " productos"
                    color: Theme.textSecondary
                }
                
                // Vista toggle
                Row {
                    spacing: 4
                    
                    ToolButton {
                        text: "☰"
                        checked: viewMode.currentIndex === 0
                        onClicked: viewMode.currentIndex = 0
                    }
                    
                    ToolButton {
                        text: "⊞"
                        checked: viewMode.currentIndex === 1
                        onClicked: viewMode.currentIndex = 1
                    }
                }
            }
        }
        
        //----------------------------------------------------------------------
        // Vista de productos (lista o grid)
        //----------------------------------------------------------------------
        StackLayout {
            id: viewMode
            Layout.fillWidth: true
            Layout.fillHeight: true
            currentIndex: 0
            
            // Vista de lista
            Pane {
                Material.elevation: 1
                Material.background: Theme.card
                padding: 0
                
                ListView {
                    id: productListView
                    anchors.fill: parent
                    clip: true
                    
                    model: InventoryService.products
                    
                    // Header
                    header: Rectangle {
                        width: productListView.width
                        height: 48
                        color: Theme.surfaceVariant
                        
                        RowLayout {
                            anchors.fill: parent
                            anchors.leftMargin: Theme.spacingLg
                            anchors.rightMargin: Theme.spacingLg
                            spacing: Theme.spacingSm
                            
                            Label {
                                text: "Producto"
                                font.weight: Theme.fontWeightSemiBold
                                Layout.preferredWidth: 250
                            }
                            Label {
                                text: "Categoría"
                                font.weight: Theme.fontWeightSemiBold
                                Layout.preferredWidth: 120
                            }
                            Label {
                                text: "Costo USD"
                                font.weight: Theme.fontWeightSemiBold
                                Layout.preferredWidth: 100
                                horizontalAlignment: Text.AlignRight
                            }
                            Label {
                                text: "Margen"
                                font.weight: Theme.fontWeightSemiBold
                                Layout.preferredWidth: 80
                                horizontalAlignment: Text.AlignRight
                            }
                            Label {
                                text: "Precio Venta"
                                font.weight: Theme.fontWeightSemiBold
                                Layout.preferredWidth: 130
                                horizontalAlignment: Text.AlignRight
                            }
                            Label {
                                text: "Stock"
                                font.weight: Theme.fontWeightSemiBold
                                Layout.preferredWidth: 80
                                horizontalAlignment: Text.AlignCenter
                            }
                            Item { Layout.fillWidth: true }
                            Label {
                                text: "Acciones"
                                font.weight: Theme.fontWeightSemiBold
                                Layout.preferredWidth: 80
                            }
                        }
                    }
                    
                    delegate: Rectangle {
                        width: productListView.width
                        height: 56
                        color: index % 2 === 0 ? Theme.card : Theme.surfaceVariant
                        
                        // Indicador de stock
                        Rectangle {
                            width: 4
                            height: parent.height
                            color: {
                                if (model.stockStatus === "out") return Theme.error
                                if (model.stockStatus === "low") return Theme.warning
                                return "transparent"
                            }
                        }
                        
                        RowLayout {
                            anchors.fill: parent
                            anchors.leftMargin: Theme.spacingLg
                            anchors.rightMargin: Theme.spacingLg
                            spacing: Theme.spacingSm
                            
                            ColumnLayout {
                                Layout.preferredWidth: 250
                                spacing: 2
                                
                                Label {
                                    text: model.name
                                    font.weight: Theme.fontWeightMedium
                                    elide: Text.ElideRight
                                    Layout.fillWidth: true
                                }
                                
                                Label {
                                    text: model.sku || model.barcode || ""
                                    font.pixelSize: Theme.fontSizeXs
                                    color: Theme.textSecondary
                                    visible: text !== ""
                                }
                            }
                            
                            Label {
                                text: model.category || "-"
                                Layout.preferredWidth: 120
                                elide: Text.ElideRight
                                color: Theme.textSecondary
                            }
                            
                            Label {
                                text: model.formattedCostUsd
                                Layout.preferredWidth: 100
                                horizontalAlignment: Text.AlignRight
                                font.family: "monospace"
                            }
                            
                            Label {
                                text: model.marginPercent.toFixed(1) + "%"
                                Layout.preferredWidth: 80
                                horizontalAlignment: Text.AlignRight
                                color: model.marginPercent >= 30 ? Theme.success : Theme.textSecondary
                            }
                            
                            Label {
                                text: model.formattedSalePrice
                                Layout.preferredWidth: 130
                                horizontalAlignment: Text.AlignRight
                                font.weight: Theme.fontWeightBold
                                color: Theme.primary
                            }
                            
                            Rectangle {
                                Layout.preferredWidth: 80
                                Layout.preferredHeight: 28
                                Layout.alignment: Qt.AlignCenter
                                radius: 14
                                color: {
                                    if (model.stockStatus === "out") return Theme.errorLight
                                    if (model.stockStatus === "low") return Theme.warningLight
                                    return Theme.successLight
                                }
                                
                                Label {
                                    anchors.centerIn: parent
                                    text: model.stockQuantity
                                    font.weight: Theme.fontWeightBold
                                    color: {
                                        if (model.stockStatus === "out") return Theme.error
                                        if (model.stockStatus === "low") return Theme.warning
                                        return Theme.success
                                    }
                                }
                            }
                            
                            Item { Layout.fillWidth: true }
                            
                            Row {
                                Layout.preferredWidth: 80
                                spacing: 4
                                
                                ToolButton {
                                    width: 36
                                    height: 36
                                    text: "✏️"
                                    
                                    onClicked: {
                                        editProductDialog.loadProduct(model.id)
                                        editProductDialog.open()
                                    }
                                    
                                    ToolTip.visible: hovered
                                    ToolTip.text: "Editar"
                                }
                                
                                ToolButton {
                                    width: 36
                                    height: 36
                                    text: "🗑️"
                                    
                                    onClicked: {
                                        deleteConfirmDialog.productId = model.id
                                        deleteConfirmDialog.productName = model.name
                                        deleteConfirmDialog.open()
                                    }
                                    
                                    ToolTip.visible: hovered
                                    ToolTip.text: "Eliminar"
                                }
                            }
                        }
                        
                        // Hover effect
                        MouseArea {
                            anchors.fill: parent
                            hoverEnabled: true
                            propagateComposedEvents: true
                            
                            onEntered: parent.color = Theme.primaryContainer
                            onExited: parent.color = index % 2 === 0 ? Theme.card : Theme.surfaceVariant
                            onClicked: mouse.accepted = false
                        }
                    }
                    
                    // Empty state
                    Label {
                        anchors.centerIn: parent
                        text: "📦 No hay productos\nHaz clic en + para agregar uno"
                        horizontalAlignment: Text.AlignHCenter
                        color: Theme.textSecondary
                        visible: productListView.count === 0
                    }
                }
            }
            
            // Vista de grid
            Pane {
                Material.elevation: 1
                Material.background: Theme.background
                
                GridView {
                    id: productGridView
                    anchors.fill: parent
                    clip: true
                    
                    cellWidth: 180
                    cellHeight: 170
                    
                    model: InventoryService.products
                    
                    delegate: ProductCard {
                        width: productGridView.cellWidth - Theme.spacingMd
                        height: productGridView.cellHeight - Theme.spacingMd
                        
                        productName: model.name
                        category: model.category || ""
                        formattedPrice: model.formattedSalePrice
                        stockQuantity: model.stockQuantity
                        stockStatus: model.stockStatus
                        
                        onClicked: {
                            editProductDialog.loadProduct(model.id)
                            editProductDialog.open()
                        }
                    }
                }
            }
        }
    }
    
    //==========================================================================
    // FAB - Agregar producto
    //==========================================================================
    FloatingActionButton {
        anchors.right: parent.right
        anchors.bottom: parent.bottom
        anchors.margins: Theme.spacingXl
        
        iconText: "+"
        
        onClicked: addProductDialog.open()
    }
    
    //==========================================================================
    // Diálogo: Agregar Producto
    //==========================================================================
    Dialog {
        id: addProductDialog
        title: "Nuevo Producto"
        modal: true
        anchors.centerIn: parent
        width: 500
        
        standardButtons: Dialog.Ok | Dialog.Cancel
        
        onAccepted: {
            let id = InventoryService.addProduct(
                addNameField.text,
                addDescField.text,
                addCategoryField.editText,
                parseFloat(addCostField.text.replace(",", ".")) || 0,
                parseFloat(addMarginField.text) || 30,
                parseInt(addStockField.text) || 0,
                addSkuField.text,
                addBarcodeField.text
            )
            
            if (id > 0) {
                clearAddForm()
                toast.show("Producto agregado correctamente", "success")
            }
        }
        
        function clearAddForm() {
            addNameField.text = ""
            addDescField.text = ""
            addCategoryField.editText = ""
            addCostField.text = ""
            addMarginField.text = "30"
            addStockField.text = "0"
            addSkuField.text = ""
            addBarcodeField.text = ""
        }
        
        onOpened: addNameField.forceActiveFocus()
        
        ColumnLayout {
            anchors.fill: parent
            spacing: Theme.spacingMd
            
            GridLayout {
                columns: 2
                columnSpacing: Theme.spacingMd
                rowSpacing: Theme.spacingSm
                Layout.fillWidth: true
                
                Label { text: "Nombre *" }
                TextField {
                    id: addNameField
                    Layout.fillWidth: true
                    placeholderText: "Nombre del producto"
                }
                
                Label { text: "Descripción" }
                TextField {
                    id: addDescField
                    Layout.fillWidth: true
                    placeholderText: "Descripción opcional"
                }
                
                Label { text: "Categoría" }
                ComboBox {
                    id: addCategoryField
                    Layout.fillWidth: true
                    editable: true
                    model: InventoryService.categories
                }
                
                Label { text: "Costo USD *" }
                TextField {
                    id: addCostField
                    Layout.fillWidth: true
                    placeholderText: "0.00"
                    validator: DoubleValidator { bottom: 0; decimals: 2 }
                    
                    onTextChanged: updatePricePreview()
                }
                
                Label { text: "Margen %" }
                TextField {
                    id: addMarginField
                    Layout.fillWidth: true
                    text: "30"
                    validator: DoubleValidator { bottom: 0; top: 500; decimals: 1 }
                    
                    onTextChanged: updatePricePreview()
                }
                
                Label { text: "Stock inicial" }
                SpinBox {
                    id: addStockField
                    Layout.fillWidth: true
                    from: 0
                    to: 99999
                    value: 0
                    editable: true
                }
                
                Label { text: "SKU" }
                TextField {
                    id: addSkuField
                    Layout.fillWidth: true
                    placeholderText: "Código interno"
                }
                
                Label { text: "Código de barras" }
                TextField {
                    id: addBarcodeField
                    Layout.fillWidth: true
                    placeholderText: "EAN/UPC"
                }
            }
            
            // Preview de precio
            Pane {
                Layout.fillWidth: true
                Material.background: Theme.primaryContainer
                
                RowLayout {
                    anchors.fill: parent
                    
                    Label {
                        text: "Precio de venta:"
                        color: Theme.textSecondary
                    }
                    
                    Label {
                        id: pricePreviewLabel
                        text: CurrencyService.formatLocal(0)
                        font.pixelSize: Theme.fontSizeXl
                        font.weight: Theme.fontWeightBold
                        color: Theme.primary
                    }
                    
                    Item { Layout.fillWidth: true }
                    
                    Label {
                        text: "Tasa: " + CurrencyService.exchangeRateFormatted
                        font.pixelSize: Theme.fontSizeSm
                        color: Theme.textSecondary
                    }
                }
            }
        }
        
        function updatePricePreview() {
            let cost = parseFloat(addCostField.text.replace(",", ".")) || 0
            let margin = parseFloat(addMarginField.text) || 0
            let price = InventoryService.calculateSalePrice(cost, margin)
            pricePreviewLabel.text = CurrencyService.formatLocal(price)
        }
    }
    
    //==========================================================================
    // Diálogo: Editar Producto
    //==========================================================================
    Dialog {
        id: editProductDialog
        title: "Editar Producto"
        modal: true
        anchors.centerIn: parent
        width: 500
        
        property int productId: -1
        
        standardButtons: Dialog.Save | Dialog.Cancel
        
        function loadProduct(id) {
            productId = id
            let product = InventoryService.getProduct(id)
            if (product.id) {
                editNameField.text = product.name || ""
                editDescField.text = product.description || ""
                editCategoryField.editText = product.category || ""
                editCostField.text = product.costUsd.toString()
                editMarginField.text = product.marginPercent.toString()
                editStockField.value = product.stockQuantity
                editSkuField.text = product.sku || ""
                editBarcodeField.text = product.barcode || ""
            }
        }
        
        onAccepted: {
            let success = InventoryService.updateProduct(productId, {
                "name": editNameField.text,
                "description": editDescField.text,
                "category": editCategoryField.editText,
                "costUsd": parseFloat(editCostField.text.replace(",", ".")) || 0,
                "marginPercent": parseFloat(editMarginField.text) || 30,
                "stockQuantity": editStockField.value,
                "sku": editSkuField.text,
                "barcode": editBarcodeField.text
            })
            
            if (success) {
                toast.show("Producto actualizado", "success")
            }
        }
        
        ColumnLayout {
            anchors.fill: parent
            spacing: Theme.spacingMd
            
            GridLayout {
                columns: 2
                columnSpacing: Theme.spacingMd
                rowSpacing: Theme.spacingSm
                Layout.fillWidth: true
                
                Label { text: "Nombre *" }
                TextField { id: editNameField; Layout.fillWidth: true }
                
                Label { text: "Descripción" }
                TextField { id: editDescField; Layout.fillWidth: true }
                
                Label { text: "Categoría" }
                ComboBox {
                    id: editCategoryField
                    Layout.fillWidth: true
                    editable: true
                    model: InventoryService.categories
                }
                
                Label { text: "Costo USD *" }
                TextField {
                    id: editCostField
                    Layout.fillWidth: true
                    validator: DoubleValidator { bottom: 0; decimals: 2 }
                }
                
                Label { text: "Margen %" }
                TextField {
                    id: editMarginField
                    Layout.fillWidth: true
                    validator: DoubleValidator { bottom: 0; top: 500; decimals: 1 }
                }
                
                Label { text: "Stock" }
                SpinBox {
                    id: editStockField
                    Layout.fillWidth: true
                    from: 0
                    to: 99999
                    editable: true
                }
                
                Label { text: "SKU" }
                TextField { id: editSkuField; Layout.fillWidth: true }
                
                Label { text: "Código de barras" }
                TextField { id: editBarcodeField; Layout.fillWidth: true }
            }
        }
    }
    
    //==========================================================================
    // Diálogo: Confirmar eliminación
    //==========================================================================
    Dialog {
        id: deleteConfirmDialog
        title: "Confirmar Eliminación"
        modal: true
        anchors.centerIn: parent
        
        property int productId: -1
        property string productName: ""
        
        standardButtons: Dialog.Yes | Dialog.No
        
        Label {
            text: "¿Eliminar el producto \"" + deleteConfirmDialog.productName + "\"?"
            wrapMode: Text.WordWrap
        }
        
        onAccepted: {
            InventoryService.deleteProduct(productId)
            toast.show("Producto eliminado", "success")
        }
    }
    
    //==========================================================================
    // Toast para notificaciones
    //==========================================================================
    Toast {
        id: toast
        anchors.horizontalCenter: parent.horizontalCenter
        anchors.top: parent.top
    }
}
```

---

## 📄 src/ui/qml/views/SalesView.qml

```qml
//==============================================================================
// SalesView.qml
// Vento - Punto de Venta (POS)
//==============================================================================

import QtQuick
import QtQuick.Controls
import QtQuick.Controls.Material
import QtQuick.Layouts

import Vento.Sales 1.0
import Vento.Inventory 1.0
import Vento.Currency 1.0

import "../theme"
import "../components"

Item {
    id: salesView
    
    RowLayout {
        anchors.fill: parent
        spacing: Theme.spacingLg
        
        //======================================================================
        // Panel izquierdo: Productos
        //======================================================================
        ColumnLayout {
            Layout.fillWidth: true
            Layout.fillHeight: true
            Layout.preferredWidth: 2
            spacing: Theme.spacingMd
            
            // Búsqueda
            SearchBar {
                Layout.fillWidth: true
                placeholder: "🔍 Buscar producto o escanear código..."
                
                onSearchTriggered: (text) => {
                    if (text.length >= 2) {
                        InventoryService.products.search(text)
                    } else if (text.length === 0) {
                        InventoryService.products.load()
                    }
                }
            }
            
            // Grid de productos
            Pane {
                Layout.fillWidth: true
                Layout.fillHeight: true
                Material.elevation: 1
                Material.background: Theme.background
                padding: Theme.spacingSm
                
                GridView {
                    id: productGrid
                    anchors.fill: parent
                    clip: true
                    
                    cellWidth: 160
                    cellHeight: 150
                    
                    model: InventoryService.products
                    
                    delegate: ProductCard {
                        width: productGrid.cellWidth - Theme.spacingSm
                        height: productGrid.cellHeight - Theme.spacingSm
                        
                        productName: model.name
                        category: model.category || ""
                        formattedPrice: model.formattedSalePrice
                        stockQuantity: model.stockQuantity
                        stockStatus: model.stockStatus
                        
                        onClicked: {
                            if (model.stockQuantity > 0) {
                                SalesService.addToCart(model.id)
                            }
                        }
                    }
                    
                    Label {
                        anchors.centerIn: parent
                        text: "No se encontraron productos"
                        color: Theme.textSecondary
                        visible: productGrid.count === 0
                    }
                }
            }
        }
        
        //======================================================================
        // Panel derecho: Carrito
        //======================================================================
        ColumnLayout {
            Layout.preferredWidth: 380
            Layout.fillHeight: true
            spacing: Theme.spacingMd
            
            // Header del carrito
            Pane {
                Layout.fillWidth: true
                Material.elevation: 2
                Material.background: Theme.primary
                
                RowLayout {
                    anchors.fill: parent
                    
                    Label {
                        text: "🛒 Carrito"
                        font.pixelSize: Theme.fontSizeLg
                        font.weight: Theme.fontWeightBold
                        color: Theme.textOnPrimary
                    }
                    
                    Item { Layout.fillWidth: true }
                    
                    Label {
                        text: SalesService.cart.totalItems + " items"
                        color: Theme.textOnPrimary
                        opacity: 0.9
                    }
                    
                    ToolButton {
                        text: "🗑️"
                        visible: !SalesService.cart.isEmpty
                        
                        onClicked: SalesService.clearCart()
                        
                        ToolTip.visible: hovered
                        ToolTip.text: "Vaciar carrito"
                    }
                }
            }
            
            // Lista de items
            Pane {
                Layout.fillWidth: true
                Layout.fillHeight: true
                Material.elevation: 1
                Material.background: Theme.card
                padding: 0
                
                ListView {
                    id: cartList
                    anchors.fill: parent
                    clip: true
                    
                    model: SalesService.cart
                    
                    delegate: Rectangle {
                        width: cartList.width
                        height: 72
                        color: index % 2 === 0 ? Theme.card : Theme.surfaceVariant
                        
                        RowLayout {
                            anchors.fill: parent
                            anchors.margins: Theme.spacingMd
                            spacing: Theme.spacingSm
                            
                            ColumnLayout {
                                Layout.fillWidth: true
                                spacing: 2
                                
                                Label {
                                    text: model.productName
                                    font.weight: Theme.fontWeightMedium
                                    elide: Text.ElideRight
                                    Layout.fillWidth: true
                                }
                                
                                Label {
                                    text: CurrencyService.formatLocal(model.unitPrice) + " c/u"
                                    font.pixelSize: Theme.fontSizeSm
                                    color: Theme.textSecondary
                                }
                            }
                            
                            // Controles de cantidad
                            Row {
                                spacing: Theme.spacingXs
                                
                                RoundButton {
                                    width: 32
                                    height: 32
                                    text: "-"
                                    font.pixelSize: Theme.fontSizeLg
                                    
                                    Material.background: Theme.surfacePressed
                                    
                                    onClicked: SalesService.cart.decrementQuantity(index)
                                }
                                
                                Label {
                                    width: 36
                                    text: model.quantity
                                    font.weight: Theme.fontWeightBold
                                    font.pixelSize: Theme.fontSizeLg
                                    horizontalAlignment: Text.AlignHCenter
                                    anchors.verticalCenter: parent.verticalCenter
                                }
                                
                                RoundButton {
                                    width: 32
                                    height: 32
                                    text: "+"
                                    font.pixelSize: Theme.fontSizeLg
                                    enabled: model.quantity < model.maxStock
                                    
                                    Material.background: Theme.primary
                                    Material.foreground: Theme.textOnPrimary
                                    
                                    onClicked: SalesService.cart.incrementQuantity(index)
                                }
                            }
                            
                            // Subtotal
                            Label {
                                text: CurrencyService.formatLocal(model.subtotal)
                                font.weight: Theme.fontWeightBold
                                color: Theme.primary
                                Layout.preferredWidth: 100
                                horizontalAlignment: Text.AlignRight
                            }
                            
                            // Eliminar
                            ToolButton {
                                text: "✕"
                                onClicked: SalesService.cart.removeItem(index)
                            }
                        }
                    }
                    
                    // Empty state
                    ColumnLayout {
                        anchors.centerIn: parent
                        spacing: Theme.spacingMd
                        visible: cartList.count === 0
                        
                        Label {
                            text: "🛒"
                            font.pixelSize: 48
                            Layout.alignment: Qt.AlignHCenter
                        }
                        
                        Label {
                            text: "Carrito vacío"
                            font.pixelSize: Theme.fontSizeLg
                            color: Theme.textSecondary
                            Layout.alignment: Qt.AlignHCenter
                        }
                        
                        Label {
                            text: "Selecciona productos para añadirlos"
                            font.pixelSize: Theme.fontSizeSm
                            color: Theme.textSecondary
                            Layout.alignment: Qt.AlignHCenter
                        }
                    }
                }
            }
            
            // Totales
            Pane {
                Layout.fillWidth: true
                Material.elevation: 1
                Material.background: Theme.card
                
                ColumnLayout {
                    anchors.fill: parent
                    spacing: Theme.spacingSm
                    
                    RowLayout {
                        Layout.fillWidth: true
                        
                        Label {
                            text: "Subtotal"
                            color: Theme.textSecondary
                        }
                        
                        Item { Layout.fillWidth: true }
                        
                        Label {
                            text: CurrencyService.formatLocal(SalesService.cart.subtotal)
                        }
                    }
                    
                    RowLayout {
                        Layout.fillWidth: true
                        
                        Label {
                            text: "IVA (" + SalesService.cart.taxPercent + "%)"
                            color: Theme.textSecondary
                        }
                        
                        Item { Layout.fillWidth: true }
                        
                        Label {
                            text: CurrencyService.formatLocal(SalesService.cart.taxAmount)
                        }
                    }
                    
                    Rectangle {
                        Layout.fillWidth: true
                        height: 1
                        color: Theme.divider
                    }
                    
                    RowLayout {
                        Layout.fillWidth: true
                        
                        Label {
                            text: "TOTAL"
                            font.pixelSize: Theme.fontSizeXl
                            font.weight: Theme.fontWeightBold
                        }
                        
                        Item { Layout.fillWidth: true }
                        
                        Label {
                            text: CurrencyService.formatLocal(SalesService.cart.total)
                            font.pixelSize: Theme.fontSizeXxl
                            font.weight: Theme.fontWeightBold
                            color: Theme.primary
                        }
                    }
                }
            }
            
            // Botones de pago
            GridLayout {
                Layout.fillWidth: true
                columns: 2
                columnSpacing: Theme.spacingSm
                rowSpacing: Theme.spacingSm
                
                Button {
                    Layout.fillWidth: true
                    text: "💵 Efectivo"
                    enabled: !SalesService.cart.isEmpty
                    Material.background: Theme.success
                    Material.foreground: Theme.textOnPrimary
                    
                    onClicked: processSale("cash")
                }
                
                Button {
                    Layout.fillWidth: true
                    text: "💳 Tarjeta"
                    enabled: !SalesService.cart.isEmpty
                    Material.background: Theme.primary
                    Material.foreground: Theme.textOnPrimary
                    
                    onClicked: processSale("card")
                }
                
                Button {
                    Layout.fillWidth: true
                    text: "📱 Pago Móvil"
                    enabled: !SalesService.cart.isEmpty
                    Material.background: Theme.info
                    Material.foreground: Theme.textOnPrimary
                    
                    onClicked: processSale("mobile")
                }
                
                Button {
                    Layout.fillWidth: true
                    text: "🔄 Transferencia"
                    enabled: !SalesService.cart.isEmpty
                    
                    onClicked: processSale("transfer")
                }
            }
        }
    }
    
    //==========================================================================
    // Funciones
    //==========================================================================
    function processSale(paymentMethod) {
        confirmSaleDialog.paymentMethod = paymentMethod
        confirmSaleDialog.open()
    }
    
    //==========================================================================
    // Diálogos
    //==========================================================================
    Dialog {
        id: confirmSaleDialog
        title: "Confirmar Venta"
        modal: true
        anchors.centerIn: parent
        width: 400
        
        property string paymentMethod: "cash"
        
        standardButtons: Dialog.Ok | Dialog.Cancel
        
        onAccepted: {
            let saleId = SalesService.checkoutCart(paymentMethod, customerName.text)
            
            if (saleId > 0) {
                saleSuccessDialog.saleId = saleId
                saleSuccessDialog.total = SalesService.cart.total
                saleSuccessDialog.open()
            }
            
            customerName.text = ""
        }
        
        ColumnLayout {
            anchors.fill: parent
            spacing: Theme.spacingLg
            
            Label {
                text: "Total a cobrar:"
                font.pixelSize: Theme.fontSizeMd
                color: Theme.textSecondary
            }
            
            Label {
                text: CurrencyService.formatLocal(SalesService.cart.total)
                font.pixelSize: Theme.fontSizeDisplay
                font.weight: Theme.fontWeightBold
                color: Theme.primary
                Layout.alignment: Qt.AlignHCenter
            }
            
            TextField {
                id: customerName
                Layout.fillWidth: true
                placeholderText: "Nombre del cliente (opcional)"
            }
        }
    }
    
    Dialog {
        id: saleSuccessDialog
        title: "✓ Venta Completada"
        modal: true
        anchors.centerIn: parent
        
        property int saleId: 0
        property double total: 0
        
        standardButtons: Dialog.Ok
        
        ColumnLayout {
            anchors.fill: parent
            spacing: Theme.spacingLg
            
            Label {
                text: "🎉"
                font.pixelSize: 64
                Layout.alignment: Qt.AlignHCenter
            }
            
            Label {
                text: "Venta #" + saleSuccessDialog.saleId
                font.pixelSize: Theme.fontSizeXl
                font.weight: Theme.fontWeightBold
                Layout.alignment: Qt.AlignHCenter
            }
            
            Label {
                text: CurrencyService.formatLocal(saleSuccessDialog.total)
                font.pixelSize: Theme.fontSizeXxl
                font.weight: Theme.fontWeightBold
                color: Theme.success
                Layout.alignment: Qt.AlignHCenter
            }
        }
    }
    
    //==========================================================================
    // Toast
    //==========================================================================
    Toast {
        id: toast
        anchors.horizontalCenter: parent.horizontalCenter
        anchors.top: parent.top
    }
    
    //==========================================================================
    // Conexiones
    //==========================================================================
    Connections {
        target: SalesService
        
        function onSaleCompleted(saleId, total) {
            toast.show("Venta #" + saleId + " registrada", "success")
        }
        
        function onErrorOccurred(error) {
            toast.show(error, "error")
        }
    }
}
```

---

## 📄 src/ui/qml/views/SettingsView.qml

```qml
//==============================================================================
// SettingsView.qml
// Vento - Vista de Configuración
//==============================================================================

import QtQuick
import QtQuick.Controls
import QtQuick.Controls.Material
import QtQuick.Layouts

import Vento.Currency 1.0

import "../theme"
import "../components"

ScrollView {
    id: settingsView
    
    contentWidth: availableWidth
    
    ColumnLayout {
        width: Math.min(parent.width, 800)
        anchors.horizontalCenter: parent.horizontalCenter
        spacing: Theme.spacingXl
        
        //======================================================================
        // Header
        //======================================================================
        Label {
            text: "⚙️ Configuración"
            font.pixelSize: Theme.fontSizeDisplay
            font.weight: Theme.fontWeightBold
            color: Theme.textPrimary
        }
        
        //======================================================================
        // Sección: Tasa de Cambio
        //======================================================================
        Pane {
            Layout.fillWidth: true
            Material.elevation: 1
            Material.background: Theme.card
            
            ColumnLayout {
                anchors.fill: parent
                spacing: Theme.spacingLg
                
                // Header de sección
                RowLayout {
                    Layout.fillWidth: true
                    spacing: Theme.spacingSm
                    
                    Label {
                        text: "💱"
                        font.pixelSize: Theme.fontSizeXl
                    }
                    
                    ColumnLayout {
                        spacing: 2
                        
                        Label {
                            text: "Tasa de Cambio"
                            font.pixelSize: Theme.fontSizeLg
                            font.weight: Theme.fontWeightSemiBold
                        }
                        
                        Label {
                            text: "Configura el precio del dólar"
                            font.pixelSize: Theme.fontSizeSm
                            color: Theme.textSecondary
                        }
                    }
                }
                
                Rectangle {
                    Layout.fillWidth: true
                    height: 1
                    color: Theme.divider
                }
                
                // Tasa actual
                Pane {
                    Layout.fillWidth: true
                    Material.background: Theme.primaryContainer
                    
                    RowLayout {
                        anchors.fill: parent
                        
                        ColumnLayout {
                            spacing: Theme.spacingXs
                            
                            Label {
                                text: "Tasa actual"
                                font.pixelSize: Theme.fontSizeSm
                                color: Theme.textSecondary
                            }
                            
                            RowLayout {
                                spacing: Theme.spacingSm
                                
                                Label {
                                    text: CurrencyService.localCurrencySymbol
                                    font.pixelSize: Theme.fontSizeXl
                                    color: Theme.primary
                                }
                                
                                Label {
                                    text: CurrencyService.exchangeRateFormatted
                                    font.pixelSize: Theme.fontSizeDisplay
                                    font.weight: Theme.fontWeightBold
                                    color: Theme.primary
                                }
                                
                                Label {
                                    text: "por $1 USD"
                                    font.pixelSize: Theme.fontSizeMd
                                    color: Theme.textSecondary
                                    anchors.bottom: parent.bottom
                                }
                            }
                        }
                        
                        Item { Layout.fillWidth: true }
                        
                        ColumnLayout {
                            spacing: 2
                            
                            Row {
                                spacing: Theme.spacingXs
                                
                                Label {
                                    text: {
                                        if (CurrencyService.trend === "up") return "↑"
                                        if (CurrencyService.trend === "down") return "↓"
                                        return ""
                                    }
                                    color: CurrencyService.trend === "up" ? Theme.error : Theme.success
                                    font.weight: Theme.fontWeightBold
                                    visible: CurrencyService.trend !== "stable"
                                }
                                
                                Label {
                                    text: Math.abs(CurrencyService.changePercent).toFixed(2) + "%"
                                    color: CurrencyService.trend === "up" ? Theme.error : Theme.success
                                    font.weight: Theme.fontWeightMedium
                                    visible: CurrencyService.trend !== "stable"
                                }
                            }
                            
                            Label {
                                text: "Última actualización"
                                font.pixelSize: Theme.fontSizeXs
                                color: Theme.textSecondary
                            }
                            
                            Label {
                                text: CurrencyService.lastUpdateFormatted
                                font.pixelSize: Theme.fontSizeSm
                                color: Theme.textSecondary
                            }
                        }
                    }
                }
                
                // Formulario de actualización
                Label {
                    text: "Actualizar tasa"
                    font.weight: Theme.fontWeightMedium
                }
                
                RowLayout {
                    Layout.fillWidth: true
                    spacing: Theme.spacingMd
                    
                    TextField {
                        id: newRateField
                        Layout.preferredWidth: 200
                        text: CurrencyService.exchangeRateFormatted
                        placeholderText: "Nueva tasa"
                        validator: DoubleValidator { bottom: 0.01; top: 1000000; decimals: 4 }
                        
                        Label {
                            anchors.right: parent.right
                            anchors.rightMargin: Theme.spacingMd
                            anchors.verticalCenter: parent.verticalCenter
                            text: CurrencyService.localCurrencySymbol
                            color: Theme.textSecondary
                        }
                    }
                    
                    ComboBox {
                        id: sourceCombo
                        Layout.preferredWidth: 180
                        model: [
                            { text: "Manual", value: "manual" },
                            { text: "BCV", value: "bcv" },
                            { text: "DolarToday", value: "dolartoday" },
                            { text: "Binance P2P", value: "binance" }
                        ]
                        textRole: "text"
                        valueRole: "value"
                    }
                    
                    Button {
                        text: "Actualizar"
                        Material.background: Theme.primary
                        Material.foreground: Theme.textOnPrimary
                        
                        onClicked: {
                            let rate = parseFloat(newRateField.text.replace(",", "."))
                            if (!isNaN(rate) && rate > 0) {
                                if (CurrencyService.updateRateFromSource(rate, sourceCombo.currentValue)) {
                                    toast.show("Tasa actualizada correctamente", "success")
                                }
                            }
                        }
                    }
                    
                    Item { Layout.fillWidth: true }
                }
                
                // Conversor rápido
                Rectangle {
                    Layout.fillWidth: true
                    height: 1
                    color: Theme.divider
                }
                
                Label {
                    text: "Conversor rápido"
                    font.weight: Theme.fontWeightMedium
                }
                
                RowLayout {
                    Layout.fillWidth: true
                    spacing: Theme.spacingXl
                    
                    // USD a Local
                    ColumnLayout {
                        Layout.fillWidth: true
                        spacing: Theme.spacingSm
                        
                        Label {
                            text: "USD → " + CurrencyService.localCurrencySymbol
                            color: Theme.textSecondary
                        }
                        
                        RowLayout {
                            spacing: Theme.spacingSm
                            
                            TextField {
                                id: usdInput
                                Layout.preferredWidth: 120
                                placeholderText: "0.00"
                                validator: DoubleValidator { bottom: 0; decimals: 2 }
                                
                                onTextChanged: {
                                    let val = parseFloat(text.replace(",", ".")) || 0
                                    localOutput.text = CurrencyService.convertToLocal(val).toFixed(2)
                                }
                            }
                            
                            Label { text: "→"; font.pixelSize: Theme.fontSizeXl }
                            
                            TextField {
                                id: localOutput
                                Layout.preferredWidth: 150
                                readOnly: true
                                text: "0.00"
                                
                                background: Rectangle {
                                    radius: Theme.radiusSm
                                    color: Theme.surfaceVariant
                                }
                            }
                        }
                    }
                    
                    Rectangle {
                        Layout.preferredWidth: 1
                        Layout.fillHeight: true
                        color: Theme.divider
                    }
                    
                    // Local a USD
                    ColumnLayout {
                        Layout.fillWidth: true
                        spacing: Theme.spacingSm
                        
                        Label {
                            text: CurrencyService.localCurrencySymbol + " → USD"
                            color: Theme.textSecondary
                        }
                        
                        RowLayout {
                            spacing: Theme.spacingSm
                            
                            TextField {
                                id: localInput
                                Layout.preferredWidth: 150
                                placeholderText: "0.00"
                                validator: DoubleValidator { bottom: 0; decimals: 2 }
                                
                                onTextChanged: {
                                    let val = parseFloat(text.replace(",", ".")) || 0
                                    usdOutput.text = CurrencyService.convertToUsd(val).toFixed(2)
                                }
                            }
                            
                            Label { text: "→"; font.pixelSize: Theme.fontSizeXl }
                            
                            TextField {
                                id: usdOutput
                                Layout.preferredWidth: 120
                                readOnly: true
                                text: "0.00"
                                
                                background: Rectangle {
                                    radius: Theme.radiusSm
                                    color: Theme.surfaceVariant
                                }
                            }
                        }
                    }
                }
            }
        }
        
        //======================================================================
        // Sección: Información del Negocio
        //======================================================================
        Pane {
            Layout.fillWidth: true
            Material.elevation: 1
            Material.background: Theme.card
            
            ColumnLayout {
                anchors.fill: parent
                spacing: Theme.spacingLg
                
                RowLayout {
                    spacing: Theme.spacingSm
                    
                    Label { text: "🏪"; font.pixelSize: Theme.fontSizeXl }
                    
                    Label {
                        text: "Información del Negocio"
                        font.pixelSize: Theme.fontSizeLg
                        font.weight: Theme.fontWeightSemiBold
                    }
                }
                
                Rectangle {
                    Layout.fillWidth: true
                    height: 1
                    color: Theme.divider
                }
                
                GridLayout {
                    Layout.fillWidth: true
                    columns: 2
                    columnSpacing: Theme.spacingLg
                    rowSpacing: Theme.spacingMd
                    
                    Label { text: "Nombre del negocio" }
                    TextField {
                        Layout.fillWidth: true
                        text: "Mi Tienda"
                        placeholderText: "Nombre de tu negocio"
                    }
                    
                    Label { text: "Símbolo de moneda local" }
                    TextField {
                        Layout.preferredWidth: 100
                        text: CurrencyService.localCurrencySymbol
                        
                        onEditingFinished: {
                            CurrencyService.localCurrencySymbol = text
                        }
                    }
                    
                    Label { text: "IVA por defecto (%)" }
                    SpinBox {
                        from: 0
                        to: 50
                        value: 16
                        editable: true
                    }
                }
            }
        }
        
        //======================================================================
        // Sección: Acerca de
        //======================================================================
        Pane {
            Layout.fillWidth: true
            Material.elevation: 1
            Material.background: Theme.card
            
            ColumnLayout {
                anchors.fill: parent
                spacing: Theme.spacingMd
                
                RowLayout {
                    spacing: Theme.spacingSm
                    
                    Label { text: "ℹ️"; font.pixelSize: Theme.fontSizeXl }
                    
                    Label {
                        text: "Acerca de Vento"
                        font.pixelSize: Theme.fontSizeLg
                        font.weight: Theme.fontWeightSemiBold
                    }
                }
                
                Rectangle {
                    Layout.fillWidth: true
                    height: 1
                    color: Theme.divider
                }
                
                RowLayout {
                    Layout.fillWidth: true
                    spacing: Theme.spacingLg
                    
                    Rectangle {
                        width: 64
                        height: 64
                        radius: Theme.radiusLg
                        color: Theme.primary
                        
                        Label {
                            anchors.centerIn: parent
                            text: "🌊"
                            font.pixelSize: 32
                        }
                    }
                    
                    ColumnLayout {
                        spacing: 4
                        
                        Label {
                            text: "Vento"
                            font.pixelSize: Theme.fontSizeXl
                            font.weight: Theme.fontWeightBold
                        }
                        
                        Label {
                            text: "Versión " + (typeof appVersion !== 'undefined' ? appVersion : "1.0.0")
                            color: Theme.textSecondary
                        }
                        
                        Label {
                            text: "Sistema de gestión de inventario y ventas"
                            color: Theme.textSecondary
                        }
                    }
                    
                    Item { Layout.fillWidth: true }
                }
            }
        }
        
        // Espaciado inferior
        Item { Layout.preferredHeight: Theme.spacingXl }
    }
    
    //==========================================================================
    // Toast
    //==========================================================================
    Toast {
        id: toast
        anchors.horizontalCenter: parent.horizontalCenter
        anchors.top: parent.top
    }
}
```

---

## 📄 src/ui/qml/Main.qml

```qml
//==============================================================================
// Main.qml
// Vento - Ventana principal de la aplicación
//==============================================================================

import QtQuick
import QtQuick.Window
import QtQuick.Controls
import QtQuick.Controls.Material
import QtQuick.Layouts

import Vento.Currency 1.0
import Vento.Inventory 1.0
import Vento.Sales 1.0

import "theme"
import "components"
import "views"

ApplicationWindow {
    id: mainWindow
    
    //==========================================================================
    // Propiedades de la ventana
    //==========================================================================
    width: 1400
    height: 900
    minimumWidth: 1024
    minimumHeight: 768
    visible: true
    title: "Vento - Sistema de Gestión"
    
    //==========================================================================
    // Tema Material
    //==========================================================================
    Material.theme: Material.Light
    Material.accent: Theme.primary
    Material.primary: Theme.primary
    Material.foreground: Theme.textPrimary
    Material.background: Theme.background
    
    //==========================================================================
    // Propiedades internas
    //==========================================================================
    property string currentView: "dashboard"
    property bool sidebarCollapsed: false
    
    //==========================================================================
    // Fondo
    //==========================================================================
    background: Rectangle {
        color: Theme.background
    }
    
    //==========================================================================
    // Header
    //==========================================================================
    header: AppHeader {
        sidebarCollapsed: mainWindow.sidebarCollapsed
        searchPlaceholder: {
            switch(currentView) {
                case "inventory": return "Buscar productos..."
                case "sales": return "Buscar productos para vender..."
                default: return "Buscar..."
            }
        }
        
        onMenuClicked: mainWindow.sidebarCollapsed = !mainWindow.sidebarCollapsed
        
        onSearchTextChanged: (text) => {
            if (currentView === "inventory" || currentView === "sales") {
                if (text.length >= 2) {
                    InventoryService.products.search(text)
                } else if (text.length === 0) {
                    InventoryService.products.load()
                }
            }
        }
        
        onSettingsClicked: currentView = "settings"
    }
    
    //==========================================================================
    // Contenido principal
    //==========================================================================
    RowLayout {
        anchors.fill: parent
        spacing: 0
        
        //----------------------------------------------------------------------
        // Sidebar
        //----------------------------------------------------------------------
        Sidebar {
            Layout.fillHeight: true
            collapsed: mainWindow.sidebarCollapsed
            currentView: mainWindow.currentView
            
            onNavigationRequested: (viewName) => {
                mainWindow.currentView = viewName
            }
        }
        
        //----------------------------------------------------------------------
        // Área de contenido
        //----------------------------------------------------------------------
        Rectangle {
            Layout.fillWidth: true
            Layout.fillHeight: true
            color: Theme.background
            
            // Contenedor con padding
            Item {
                anchors.fill: parent
                anchors.margins: Theme.spacingLg
                
                StackLayout {
                    anchors.fill: parent
                    currentIndex: {
                        switch(currentView) {
                            case "dashboard": return 0
                            case "sales": return 1
                            case "inventory": return 2
                            case "settings": return 3
                            default: return 0
                        }
                    }
                    
                    // Dashboard
                    DashboardView {
                        // Conexión para navegación desde dashboard
                    }
                    
                    // Ventas
                    SalesView {}
                    
                    // Inventario
                    InventoryView {}
                    
                    // Configuración
                    SettingsView {}
                }
            }
        }
    }
    
    //==========================================================================
    // Footer / Barra de estado
    //==========================================================================
    footer: ToolBar {
        height: Theme.footerHeight
        
        Material.elevation: 0
        Material.background: Theme.surface
        
        RowLayout {
            anchors.fill: parent
            anchors.leftMargin: Theme.spacingLg
            anchors.rightMargin: Theme.spacingLg
            
            // Estado de conexión
            Row {
                spacing: Theme.spacingSm
                
                Rectangle {
                    width: 8
                    height: 8
                    radius: 4
                    color: Theme.success
                    anchors.verticalCenter: parent.verticalCenter
                }
                
                Label {
                    text: "Conectado"
                    font.pixelSize: Theme.fontSizeXs
                    color: Theme.textSecondary
                }
            }
            
            Rectangle {
                width: 1
                height: 16
                color: Theme.divider
            }
            
            // Tasa de cambio
            Label {
                text: "💱 " + CurrencyService.localCurrencySymbol + " " + 
                      CurrencyService.exchangeRateFormatted
                font.pixelSize: Theme.fontSizeXs
                color: Theme.textSecondary
            }
            
            Item { Layout.fillWidth: true }
            
            // Fecha y hora
            Label {
                text: Qt.formatDateTime(new Date(), "dd/MM/yyyy hh:mm")
                font.pixelSize: Theme.fontSizeXs
                color: Theme.textSecondary
                
                Timer {
                    interval: 60000
                    running: true
                    repeat: true
                    onTriggered: parent.text = Qt.formatDateTime(new Date(), "dd/MM/yyyy hh:mm")
                }
            }
        }
    }
    
    //==========================================================================
    // Toast global
    //==========================================================================
    Toast {
        id: globalToast
        anchors.horizontalCenter: parent.horizontalCenter
        anchors.top: parent.top
        anchors.topMargin: Theme.headerHeight + Theme.spacingMd
        z: 1000
    }
    
    //==========================================================================
    // Conexiones globales
    //==========================================================================
    Connections {
        target: CurrencyService
        
        function onExchangeRateChanged(newRate) {
            globalToast.show(
                "Tasa actualizada: " + CurrencyService.localCurrencySymbol + " " + newRate.toFixed(2),
                "info"
            )
        }
    }
    
    Connections {
        target: SalesService
        
        function onSaleCompleted(saleId, total) {
            globalToast.show(
                "Venta #" + saleId + " completada: " + CurrencyService.formatLocal(total),
                "success"
            )
        }
        
        function onErrorOccurred(error) {
            globalToast.show(error, "error")
        }
    }
    
    Connections {
        target: InventoryService
        
        function onLowStockAlert(count) {
            globalToast.show("⚠️ " + count + " productos con bajo stock", "warning")
        }
    }
    
    //==========================================================================
    // Inicialización
    //==========================================================================
    Component.onCompleted: {
        console.log("═══════════════════════════════════════════════════════")
        console.log("  🌊 Vento UI iniciado")
        console.log("  Tasa: " + CurrencyService.exchangeRateFormatted)
        console.log("  Productos: " + InventoryService.totalProducts)
        console.log("═══════════════════════════════════════════════════════")
    }
}
```

---

## 📄 src/ui/qml/qml.qrc

```xml
<!DOCTYPE RCC>
<RCC version="1.0">
    <qresource prefix="/">
        <!-- Main -->
        <file>qml/Main.qml</file>
        
        <!-- Theme -->
        <file>qml/theme/Theme.qml</file>
        <file>qml/theme/qmldir</file>
        
        <!-- Components -->
        <file>qml/components/Sidebar.qml</file>
        <file>qml/components/AppHeader.qml</file>
        <file>qml/components/SearchBar.qml</file>
        <file>qml/components/StatCard.qml</file>
        <file>qml/components/ProductCard.qml</file>
        <file>qml/components/FloatingActionButton.qml</file>
        <file>qml/components/Toast.qml</file>
        
        <!-- Views -->
        <file>qml/views/DashboardView.qml</file>
        <file>qml/views/SalesView.qml</file>
        <file>qml/views/InventoryView.qml</file>
        <file>qml/views/SettingsView.qml</file>
    </qresource>
    
    <qresource prefix="/icons">
        <!-- Agregar iconos SVG aquí -->
    </qresource>
</RCC>
```

---

## 📄 src/ui/qml/theme/qmldir

```
module Theme
singleton Theme 1.0 Theme.qml
```

---

## 📄 src/ui/CMakeLists.txt (Actualizado)

```cmake
#===============================================================================
# src/ui/CMakeLists.txt
# Vento - Módulo UI
#===============================================================================

qt_add_library(VentoUI STATIC)
add_library(Vento::UI ALIAS VentoUI)

qt_add_qml_module(VentoUI
    URI Vento.UI
    VERSION 1.0
    
    QML_FILES
        qml/Main.qml
        
        # Theme
        qml/theme/Theme.qml
        
        # Components
        qml/components/Sidebar.qml
        qml/components/AppHeader.qml
        qml/components/SearchBar.qml
        qml/components/StatCard.qml
        qml/components/ProductCard.qml
        qml/components/FloatingActionButton.qml
        qml/components/Toast.qml
        
        # Views
        qml/views/DashboardView.qml
        qml/views/SalesView.qml
        qml/views/InventoryView.qml
        qml/views/SettingsView.qml
    
    RESOURCES
        qml/qml.qrc
    
    OUTPUT_DIRECTORY
        ${CMAKE_BINARY_DIR}/Vento/UI
)

target_include_directories(VentoUI
    PUBLIC
        ${CMAKE_CURRENT_SOURCE_DIR}
)

target_link_libraries(VentoUI
    PUBLIC
        Qt6::Core
        Qt6::Gui
        Qt6::Qml
        Qt6::Quick
        Qt6::QuickControls2
)
```

---

## 📊 Vista Previa de la UI

```
┌─────────────────────────────────────────────────────────────────────────────┐
│ ☰    🔍 Buscar...                              💵 USD: Bs. 36.50 ↑  🔔  ⚙️  │
├─────────────────────────────────────────────────────────────────────────────┤
│        │                                                                     │
│  🌊    │  ¡Bienvenido a Vento!                                              │
│ Vento  │  Miércoles, 15 de Enero 2025                               14:30   │
│        │                                                                     │
│ ═══════│  📈 Resumen del Día                                                │
│        │  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐              │
│ 📊     │  │ 💰 Ventas│ │ 📊 Semana│ │ 📦 Prods │ │ 💱 Tasa  │              │
│Dashboard│  │ Bs.2,450 │ │Bs.15,200 │ │    45    │ │  36.50   │              │
│        │  │ 12 ventas│ │          │ │ 3 c/bajo │ │  +2.5%   │              │
│ 🛒     │  └──────────┘ └──────────┘ └──────────┘ └──────────┘              │
│ Ventas │                                                                     │
│        │  ⚠️ Alertas de Inventario                                          │
│ 📦     │  ┌─────────────────────────────────────────────────────┐          │
│Inventario │ 3 productos con bajo stock · 1 producto agotado     │          │
│        │  └─────────────────────────────────────────────────────┘          │
│ ⚙️     │                                                                     │
│ Config │  ⚡ Acciones Rápidas                                               │
│        │  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐              │
│        │  │🛒 Nueva  │ │📦 Agregar│ │💱Actualizar│ │📊 Ver   │              │
│        │  │  Venta   │ │ Producto │ │   Tasa    │ │Reportes │              │
│        │  └──────────┘ └──────────┘ └──────────┘ └──────────┘              │
│        │                                                                     │
│ v1.0.0 │  🕐 Últimas Ventas                                                 │
│        │  ┌─────────────────────────────────────────────────────────────┐  │
│        │  │ 🛒 Venta #123  15/01/2025 14:25  3 items  Efectivo  Bs.450 │  │
│        │  │ 🛒 Venta #122  15/01/2025 13:50  1 item   Tarjeta   Bs.120 │  │
│        │  │ 🛒 Venta #121  15/01/2025 12:30  5 items  Efectivo  Bs.890 │  │
│        │  └─────────────────────────────────────────────────────────────┘  │
├─────────────────────────────────────────────────────────────────────────────┤
│ ● Conectado │ 💱 Bs. 36.50                                15/01/2025 14:30 │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## ✅ Resumen de la UI

| Componente | Descripción |
|------------|-------------|
| `Theme.qml` | Sistema de diseño con colores, espaciado, tipografía |
| `Sidebar.qml` | Navegación lateral colapsable |
| `AppHeader.qml` | Encabezado con búsqueda y acciones |
| `DashboardView.qml` | Vista principal con estadísticas |
| `SalesView.qml` | Punto de venta con carrito |
| `InventoryView.qml` | Gestión de productos |
| `SettingsView.qml` | Configuración de tasa y negocio |

---

¿Te gustaría que agregue más funcionalidades o ajuste algún componente de la UI? 🎨
